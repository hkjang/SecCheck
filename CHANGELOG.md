# SecCheck 변경 이력

이 문서는 운영자가 업그레이드 전에 확인해야 할 내용만 담습니다. 각 릴리즈의 전체 커밋 목록은
[GitHub Releases](https://github.com/hkjang/SecCheck/releases)에서 확인하세요.

표기: **주의**는 배포 전 확인이 필요한 항목, **설정**은 새로 추가되거나 의미가 바뀐 관리자 설정입니다.

여러 버전을 건너뛰어 올라오는 경우, 확인이 필요한 항목만 모아 둔 [운영 가이드의 업그레이드 표](docs/operations.md#여러-버전을-건너뛰어-올라올-때)를 먼저 보십시오.

## v1.0.17

### 수정
- **후속조치 등록부가 조용히 잘려 보이던 문제를 고쳤습니다.** 화면에 표시되는 등록부는
  500건으로 제한돼 있었는데, 그보다 많아도 아무 표시가 없었습니다. 미조치 항목이
  3,000건인 설치의 관리자는 **500건이 전부라고 믿게 됩니다** — 남은 일의 규모를
  실제보다 작게 보여 주는 화면입니다. 이제 전체 건수를 함께 계산해
  `표시 / 전체`로 보여 주고, 잘렸을 때는 안내 문구와 Excel 내보내기 안내를 띄웁니다.
- Excel 내보내기는 화면 제한(500)이 아니라 내보내기 상한(50,000행)을 적용합니다.
  전체를 담는 것이 이 파일의 용도입니다.

## v1.0.16

### 수정
- **전체 감사 체인 재검증을 한 번에 하나만 실행합니다.** 이 작업은 지금까지 쌓인
  이벤트를 전부 다시 해싱하며 그동안 연결을 붙들고 있습니다. 직전 버전에서 마감
  시간을 30분까지 늘렸으므로, 버튼을 여러 번 누르거나 여러 명이 동시에 누르면
  같은 작업이 겹쳐 돌면서 연결과 CPU를 그만큼 소모합니다. 이미 실행 중이면
  409(`VERIFY_IN_PROGRESS`)로 거절하고 완료 후 다시 시도하도록 안내합니다.
  증분 검증(일상 점검)은 제한 없이 그대로 동작합니다.

## v1.0.15

### 수정
- **데이터베이스 연결 풀에 여유를 두었습니다.** 드라이버 기본값은 CPU 수 기준이라
  작은 컨테이너에서는 **연결 4개**입니다. 알림·검사·유지보수 워커 셋이 각자 하나씩
  잡고, 직전 버전에서 마감 시간을 늘린 ZIP 내보내기나 전체 체인 재검증이 하나를
  몇 분간 붙들면, 나머지 요청은 전부 연결을 기다립니다. 이제 CPU 수 × 4, 최소
  10개를 확보하고 항상 2개를 데워 둡니다. DSN에 `pool_max_conns`/`pool_min_conns`를
  지정한 설치는 그 값을 그대로 존중합니다.
- 연결 시도에 10초 제한을 두었습니다. 접속은 받아들이지만 응답하지 않는
  데이터베이스에 요청이 무한정 매달리지 않습니다.

## v1.0.14

### 수정
- **긴 전송이 서버의 응답 마감 시간에 잘리던 문제를 고쳤습니다.** 서버는 일반 요청
  기준으로 읽기 30초·쓰기 2분 제한을 두고 있는데, 이 제한은 요청 전체에 걸립니다.
  그래서 회선이 느린 환경에서는 **25MB 증적 업로드가 진행 상태와 무관하게 30초에
  실패**하고, 첨부가 많은 심의의 ZIP 내보내기는 2분에 잘려 열리지 않는 아카이브가
  됩니다. 수백만 건이 쌓인 설치의 전체 감사 체인 재검증도 마찬가지입니다. 이제 이
  네 경로만 각각 마감 시간을 늘리며(전송 15분, 재검증 30분), 다른 요청의 제한은
  그대로입니다. 업로드 크기 제한은 종전대로 설정값이 판단합니다.

## v1.0.13

### 개선
- **결과 알림과 로딩 표시를 보조기술이 읽습니다.** 이 제품은 저장 성공도 실패도
  거의 전부 토스트로 알리는데, 토스트가 라이브 영역이 아니어서 **화면을 볼 수 없는
  사용자는 저장이 됐는지 알 방법이 없었습니다.** 이제 토스트는 `aria-live` 영역에
  놓이고 오류는 `role="alert"`, 일반 알림은 `role="status"`로 전달됩니다. 로딩
  표시도 `role="status"`가 없어 `aria-label`이 무시되고 있었습니다.
- **같은 순간에 뜬 토스트 두 개가 서로를 지우던 문제를 고쳤습니다.** 식별자를
  `Date.now()`로 만들어 1밀리초 안에 두 건이 뜨면 같은 값이 되고, 먼저 끝난 타이머가
  둘 다 지웠습니다. 순번으로 바꿨습니다.

## v1.0.12

### 개선
- **대화 상자가 키보드 사용자에게 동작하도록 고쳤습니다.** 지금까지 모달은 열려도
  포커스를 가져가지 않았습니다. 포커스는 뒤쪽 화면에 남아 있어서 Tab을 누르면
  **대화 상자가 아니라 그 뒤의 페이지**를 훑고, 닫아도 포커스가 어디로도 돌아가지
  않았습니다. 보조기술에는 대화 상자라는 사실조차 전달되지 않았습니다. 이제 열릴 때
  첫 컨트롤로 포커스가 가고, Tab이 대화 상자 안에서만 순환하며, 닫으면 열었던 자리로
  포커스가 돌아갑니다. `role="dialog"`·`aria-modal`·제목 연결도 함께 붙였습니다.
- 감사로그 상세는 공용 컴포넌트를 쓰지 않고 직접 만든 대화 상자여서 위 동작이
  하나도 없었습니다. 공용 컴포넌트로 바꾸고, 화면이 자체 대화 상자를 만들면 실패하는
  테스트를 추가했습니다.

## v1.0.11

### 개선
- **입력 항목의 라벨을 해당 컨트롤에 연결했습니다.** 지금까지 라벨은 입력 옆에 놓인
  형제 요소일 뿐 `htmlFor`로 묶여 있지 않았습니다. 그래서 화면 낭독기는 제품의
  **모든 입력 항목을 이름 없는 상자로** 읽었고, 라벨을 눌러도 포커스가 가지 않았으며,
  도움말과 오류 문구는 따로 찾아가야만 읽혔습니다. 이제 라벨·도움말·오류가 컨트롤에
  연결되고(`aria-describedby`), 오류가 있으면 `aria-invalid`, 필수 항목에는
  `aria-required`가 붙습니다. 화면 149곳의 입력이 대상이며, 컨트롤이 여럿인 항목은
  가리킬 대상이 없으므로 이전과 같이 둡니다.
- 이 연결이 사라지면 실패하는 테스트를 함께 넣었습니다.

## v1.0.10

### 개선
- **아이콘만 있는 버튼에 읽을 수 있는 이름을 붙였습니다.** Security Control 삭제,
  API 키 재발급·폐기, 템플릿 항목 삭제, MCP 주소 복사 버튼은 아이콘 하나뿐이라
  화면 낭독기에서는 "버튼"으로만 읽히고 마우스를 올려도 아무 설명이 없었습니다.
  삭제 버튼이 무엇을 지우는지 모른 채 눌러야 하는 상태였습니다. 각각 대상 이름을
  포함한 이름을 붙였습니다(예: `AC-1 Control 삭제`).
- 같은 실수가 다시 들어오지 않도록, 내용이 아이콘뿐인 버튼에 `aria-label`이나
  `title`이 없으면 실패하는 테스트를 추가했습니다.

## v1.0.9

### 수정
- **SSO 로그인 실패를 사용자가 볼 수 있게 했습니다.** 두 가지가 겹쳐 있었습니다.
  - `사내 SSO로 로그인` 버튼은 링크이므로 브라우저가 그대로 이동하는데, IdP에
    연결하지 못하면 서버가 **JSON 오류 문서**를 돌려줬습니다. 사용자는 서비스
    바깥의 기계용 출력 화면에 남겨졌습니다. 이제 로그인 화면으로 되돌리고 실패를
    `LOGIN_FAIL`(`target_type=OIDC`)로 기록합니다.
  - IdP 왕복 후 실패하면 서버는 `/login?error=...`로 되돌려 보내는데 **로그인
    화면이 그 값을 읽지 않았습니다.** 빈 로그인 폼만 나오니 사용자는 같은 버튼을
    다시 누르는 것 말고 할 수 있는 일이 없었습니다. 이제 사유를 화면에 표시하고
    (연결 실패·거부·그 외 IdP 코드), 주소창의 매개변수는 지웁니다.

## v1.0.8

### 수정
- **확인하지 못한 검사를 통과로 취급하던 지점들을 고쳤습니다.** 개수를 세는 쿼리의
  오류를 버리고 있어서, 데이터베이스가 잠깐 답하지 않으면 그 값이 0이 되고 검사는
  그대로 통과했습니다.
  - **검토 완료**: 미판정 항목 수와 미확인 보완 요청 수를 세지 못하면 그대로
    완료 처리됐습니다. 항목이 판정되지 않은 심의가 승인까지 갈 수 있었습니다.
  - **Security Control 삭제**: 연결된 체크리스트 항목 수를 세지 못하면 "연결 없음"으로
    보고 삭제를 허용했습니다.
  이제 둘 다 확인에 실패하면 작업을 중단합니다.
- **`/metrics`가 거짓 정상 신호를 보내지 않습니다.** 지표 쿼리가 실패하면 모든 값이
  0으로 나갔습니다 — 데이터베이스가 멈춘 순간에 "실패한 작업 0건, 잠긴 계정 0건,
  감사 기록 실패 0건"을 기록하는 셈입니다. 이제 503으로 응답해 스크레이프가
  실패하고 대상이 down으로 표시됩니다.

## v1.0.7

### 수정
- **목록을 읽다 실패하면 잘린 목록 대신 오류를 돌려줍니다.** 모든 목록 응답이
  거쳐 가는 행 변환기가 읽기 실패 시 `break`로 빠져나와, **그때까지 읽은 행을 전체
  결과인 것처럼** 돌려주고 있었습니다. 감사로그·심의 목록·후속조치 등록부처럼
  목록 자체가 기록인 화면에서 조용히 짧아진 결과는 오류보다 나쁩니다. 이제 읽기
  실패와 `rows.Err()`를 그대로 올려보내고, 각 조회 지점은 500으로 응답합니다
  (대시보드 통계 카드처럼 보조적인 블록은 해당 블록만 생략합니다).

## v1.0.6

### 수정
- **증적 하나를 읽지 못하면 ZIP 내보내기가 조용히 잘리던 문제를 고쳤습니다.**
  읽기 실패 시 즉시 반환했기 때문에, 사용자는 **형식상 멀쩡한 ZIP**을 받지만 그
  파일 이후의 증적이 전부 빠져 있고 그 사실을 알리는 내용은 어디에도 없었습니다.
  복원이 부분적으로 끝난 볼륨에서 정확히 이 상황이 됩니다. 이제 읽지 못한 파일은
  `evidence/EXCLUDED.txt`에 기록하고 나머지 증적은 그대로 담습니다.

## v1.0.5

### 수정
- **내보낸 심의 결과에 후속조치의 기한과 이행 상태가 빠져 있던 문제를 고쳤습니다.**
  조건부 적합은 판정과 **약속**이 한 쌍인데, 결과물에는 약속의 내용만 있고 언제까지
  무엇을 했는지가 없었습니다. PDF는 후속조치 자체도 싣지 않았습니다 — 변경 티켓에
  첨부되는 바로 그 문서입니다. 이제 JSON·Excel·PDF 모두 후속조치 내용, 조치 기한,
  이행 보고일, 이행 확인일을 담습니다. 날짜는 표시 시간대 기준입니다.

## v1.0.4

### 문서
- **API 가이드에 빠져 있던 MCP 도구 두 개를 추가했습니다.** 서버는 7개를 제공하는데
  가이드에는 5개만 적혀 있었습니다. 목록이 "제공 도구 목록"으로 제시되므로,
  `seccheck.my_queue`(지금 처리해야 할 심의와 임박한 보완 요청)와
  `seccheck.review_report`(기간별 통계, 역할 필요)는 `tools/list`를 직접 호출해 보지
  않으면 존재조차 알 수 없었습니다. 권한 범위와 쓰기 범위 규칙도 함께 적었습니다.
- **가이드와 서버가 다시 어긋나지 않도록 테스트로 묶었습니다.** 서버가 제공하는
  도구는 모두 가이드에 있어야 하고, 가이드에 적힌 도구는 모두 서버에 있어야 합니다.

## v1.0.3

### 수정
- **체크리스트 항목과 프로필 입력에도 길이 제한을 두었습니다.** 입력 경계 정리의
  마지막입니다. 항목은 그 항목이 적용되는 **모든 심의의 스냅샷으로 복사**되므로,
  한 번 길게 넣은 안내문이 이후 모든 심의에 영구히 복제됩니다. 항목코드 40자,
  제목 300자, 섹션 100자, 질문·안내·예시 4,000자, 근거 2,000자로 제한합니다.
  표시 이름(100자)·이메일(200자)·부서(100자)도 같은 이유로 제한했습니다 — 표시
  이름은 그 계정이 남기는 모든 감사 기록과 알림에 들어갑니다.
- **Excel 가져오기는 워크북을 실패시키지 않고 잘라냅니다.** 스프레드시트 셀은
  체크리스트 항목보다 훨씬 긴 값을 담을 수 있고, 셀 하나 때문에 수백 행짜리
  가져오기를 통째로 거절하는 것은 도움이 되지 않습니다. 넘치는 값은 최대 길이로
  자르고, 마법사 미리보기에 **몇 건이 잘리는지** 표시합니다.

## v1.0.2

### 수정
- **심의 자체의 입력 항목에도 길이 제한을 두었습니다.** 직전 버전에서 답변·판정
  항목을 제한했지만, 심의를 만들 때 넣는 서비스명·부서·설명은 "비어 있지 않을 것"
  외에 아무 조건이 없었습니다. 서비스명은 **이 심의에 대한 모든 알림 본문에
  복사**되므로, 한 번의 긴 입력이 알림 수십 통과 메일 수십 통으로 증폭됩니다.
  서비스명 200자, 부서 100자, 설명·최종 의견·승인 의견 각 4,000자로 제한하고,
  수정(PATCH) 경로에도 같은 제한을 적용합니다.

## v1.0.1

### 수정
- **자유 입력 항목에 길이 제한을 두었습니다.** 요청 본문 상한이 2MB이므로 현재
  상태·조치 계획·N/A 사유·검토 의견·후속조치·보완 사유 한 칸에 수십만 자를 넣을 수
  있었고, 아무도 막지 않았습니다. 그대로 DB에 저장되고, Excel 셀 상한(32,767자)을
  넘겨 **내보낸 파일이 열리지 않게** 되며, 화면에서는 스크롤조차 어려운 입력이
  됩니다. 이제 각 4,000자로 제한하고, 넘으면 어느 항목이 문제인지 알려 줍니다
  (코멘트 4,000자, 보고 메모 2,000자는 이전부터 제한이 있었습니다).
- **제한이 생기기 전에 저장된 긴 값은 내보낼 때 잘라냅니다.** Excel 셀 상한을 넘는
  값은 `…(이하 생략)`을 붙여 자릅니다. 파일이 열리지 않는 것보다 잘린 것이 낫고,
  잘렸다는 사실이 보입니다.

## v1.0.0

### 문서
- **여러 버전을 건너뛰어 올라올 때 표를 최신 상태로 채웠습니다.** 이 표는 오프라인
  설치가 반년치 릴리즈를 한 번에 넘길 때 읽는 유일한 요약인데 v0.59.0에서 멈춰
  있었습니다. 그 사이에 숫자가 달라지거나 연계 스크립트가 깨지는 변경이 여럿
  있었습니다 — 시간대 기준 변경(v0.76.0·v0.77.0), CSV 시각 형식 변경(v0.78.0),
  기한 필수화(v0.86.0·v0.87.0), 잘못된 키로 기동 거부(v0.92.0), 본인 심의 처리
  금지(v0.98.0).
- **표가 다시 뒤처지지 않도록 테스트로 묶었습니다.** CHANGELOG에 `주의` 절이 있는
  릴리즈는 업그레이드 표에 반드시 나와야 하며, 없으면 CI가 실패합니다. 운영자가
  찾아볼 곳과 우리가 적어 두는 곳이 어긋나지 않게 하는 규칙입니다.
- v0.86.0·v0.87.0에도 `주의` 절을 소급해 적었습니다. 연계 스크립트가 기한 없이
  보내면 422로 거절되는 변경이었는데 그 사실이 기록되지 않았습니다.

## v0.99.0

### 추가
- **역할별 활성 보유자 수를 시스템 정보에서 보여 줍니다.** 직전 버전에서 본인 심의
  처리를 금지했으므로, 검토자나 승인자가 한 명뿐인 설치에서는 **그 사람이 신청한
  심의를 아무도 처리할 수 없습니다**. 그 상태를 배포 후에 사고로 알게 되지 않도록,
  역할별로 활성 계정 수를 표시하고 보유자가 없으면 `담당자 없음`, 한 명뿐이면
  `1명뿐`으로 표시합니다. 비활성 계정은 행동할 수 없으므로 활성 수에서 제외되며,
  전체 수와 함께 보여 줍니다.

## v0.98.0

### 보안
- **본인이 신청한 심의를 본인이 검토·승인할 수 없습니다.** 지금까지 아무 제약이
  없었습니다. `REQUESTER`와 `APPROVER`를 함께 가진 사람은 자기 변경을 올리고 자기가
  승인할 수 있었고, 승인자가 지정되지 않은 건은 **승인자 권한을 가진 누구나**
  결재할 수 있어 신청자 본인도 포함됐습니다. 검토 쪽도 마찬가지로, `SECURITY_REVIEWER`를
  겸한 신청자가 자기 심의를 검토 시작하고 판정할 수 있었습니다. 심의를 요청한 사람이
  그 심의의 판정자가 되는 것은 이 프로세스가 막으려는 바로 그 상황입니다.
  - 검토 시작, 항목 판정, 일괄 판정, 보완 요청, 최종 승인이 모두 거부됩니다
    (`SELF_REVIEW_FORBIDDEN`).
  - 담당자가 한 명뿐인 설치를 위해 서비스 설정 > 워크플로에
    `본인이 신청한 심의를 본인이 검토·승인 허용`을 두었습니다. **기본값은 금지**이며,
    마이그레이션 `018`이 기존 설치에도 같은 기본값을 적용합니다.

### 주의
- 한 사람이 신청과 검토를 겸하도록 운영해 온 설치는 업그레이드 후 검토가 막힙니다.
  다른 검토자·승인자를 지정하거나, 위 설정을 명시적으로 켜십시오.

## v0.97.0

### 빌드
- **비밀정보 스캔 단계가 CI를 막던 문제를 고쳤습니다.** `gitleaks-action`이
  라이선스 키(`GITLEAKS_LICENSE`)를 요구하며 실패하기 시작했습니다. 이 파이프라인이
  외부 라이선스 서비스에 의존해야 할 이유가 없고, 같은 단계가 앞서 v0.82.0에서는
  그 메시지를 출력하다 러너의 로그 인코더를 죽이기도 했습니다. 이제 공식
  gitleaks 이미지를 **다이제스트로 고정**해 직접 실행합니다. 검사하는 바이너리는
  동일하며, 저장소의 다른 고정 방식과 일관됩니다.

## v0.96.0

> 이 버전은 아래 v0.97.0에서 고친 CI 문제로 이미지가 게시되지 않았습니다. 내용은
> v0.97.0 이미지에 포함되어 있습니다.

### 빌드
- **릴리즈를 막던 불안정한 테스트 단언을 제거했습니다.** 감사 체인 검증 규모 테스트가
  증분 검사와 전체 검사의 실행 시간을 직접 비교하고 있었습니다. 두 값 모두 100ms
  미만이라 공유 러너의 스케줄링 흔들림만으로 뒤집힙니다 — v0.93.0 릴리즈 빌드는
  증분 검사가 이벤트 **1건**만 읽고도 "전체 검사보다 느리다"는 이유로 실패했습니다.
  체크포인트가 동작하는지는 이미 재검사 건수로 확인하고 있으므로(3,001건 중 1건),
  시간 비교는 지우고 측정값은 로그로만 남깁니다.

## v0.95.0

### 추가
- **템플릿 버전 `사용 중지`를 화면에서 실행합니다.** 엔드포인트는 있었지만 버튼이
  없어, 쓰지 않게 된 체크리스트를 물러나게 하려면 API를 직접 호출하거나 대체 버전을
  새로 게시하는 수밖에 없었습니다. 게시된 버전은 수정할 수 없으므로 사실상 방법이
  없던 셈입니다. 템플릿 상세에서 게시 상태인 버전에 `사용 중지` 버튼이 나오며,
  이후 새 심의에 배정되지 않고 이미 만들어진 심의의 스냅샷은 그대로 유지됩니다.
- 이 동작을 검증하는 테스트를 추가했습니다: 사용 중지 전 심의에는 항목이 배정되고,
  이후 심의에는 배정되지 않으며, 먼저 만들어진 심의의 스냅샷은 바뀌지 않습니다.

### 문서
- 관리자 가이드의 `시스템 정보` 절 위치와 번호를 바로잡았습니다(14-1 → 13-1,
  스키마 버전 확인 바로 뒤).

## v0.94.0

### 추가
- **심의 참여자를 화면에서 보고 관리합니다.** 사용자 가이드는 처음부터 "심의에 사람을
  추가할 때 작성 가능과 열람 전용 중에서 선택합니다"라고 안내하고 있었지만, 그 동작을
  하는 화면이 없었습니다 — API로만 추가할 수 있었고, **누구에게 접근을 줬는지 확인할
  방법도, 회수할 방법도 없었습니다**. 접근을 주는 것이 한 방향뿐인 상태였습니다.
  - 심의 상세에 `참여자` 카드를 추가했습니다: 현재 참여자·부서·권한 표시, 추가, 해제.
  - `GET /api/v1/review-requests/{id}/participants`로 목록을 조회하고,
    `DELETE .../participants/{userID}`로 해제합니다. 해제는 `REMOVE_PARTICIPANT`
    감사 이벤트로 남습니다 — 접근을 준 사실만 기록되고 회수는 기록되지 않으면
    접근 이력이 반쪽입니다.
  - 목록은 심의 접근 권한으로, 추가·해제는 편집 권한으로 통제됩니다.

## v0.93.0

### 추가
- **증적 볼륨의 남은 공간을 보고 경고합니다.** 오프라인으로 몇 년을 돌리는 설치는
  언젠가 디스크가 찹니다. 그런데 남은 공간은 어느 화면에도, 어느 지표에도 없었고,
  처음 알게 되는 순간은 증적 업로드가 실패할 때 — 그것도 아무 조치도 할 수 없는
  신청자 쪽이었습니다.
  - `/metrics`에 `seccheck_storage_free_bytes`와 `seccheck_storage_writable`을
    추가했습니다.
  - 남은 공간이 전체의 10% 또는 2GB 아래이거나 볼륨에 파일을 만들 수 없으면
    시스템 관리자에게 `저장 공간 부족` 알림이 갑니다(6시간에 한 번).
- **시스템 정보 화면(`/admin/system`)을 추가했습니다.** 버전·스키마 버전·데이터
  규모·PDF 글꼴 가용성을 돌려주는 API는 처음부터 있었지만 **그것을 보여 주는 화면이
  없었습니다** — 운영 가이드는 "관리자 > 시스템 정보에서 확인"이라고 안내하고
  있었는데 그런 메뉴가 없었습니다. 이제 증적 저장소 상태까지 포함해 한 화면에서
  봅니다.

## v0.92.0

### 보안
- **잘못된 `ENCRYPTION_KEY`로 기동하면 시작을 거부합니다.** 지금까지는 아무 검사도
  하지 않았습니다. 다른 환경에서 복원했거나 키를 잘못 붙여넣은 경우에도 DB는
  응답하고 `/ready`는 정상이며 로그인도 됩니다. 문제는 한참 뒤 다른 곳에서
  드러납니다 — 증적이 내려받아지지 않고, 암호화된 설정(SMTP 비밀번호, OIDC
  client secret)은 조용히 비어 있는 값으로 읽힙니다. 원인에서 멀리 떨어진 증상입니다.
  이제 기동 시 이미 저장된 사용자 데이터 키를 실제로 열어 보고, 열리지 않으면
  무엇이 잘못됐는지 밝히고 종료합니다. 비어 있는 새 데이터베이스에서는 비교할
  대상이 없으므로 그대로 시작합니다.

## v0.91.0

### 추가
- **마지막 관리자 계정 복구 명령(`admin-recover`)을 추가했습니다.** 관리자가 한 명뿐인
  폐쇄망 설치에서 그 계정이 막히면(비밀번호 분실, 일회용 코드 기기 분실, 비활성화)
  돌아갈 길이 없었습니다. 다른 관리자가 풀어 주는 것이 유일한 경로인데 그 다른
  관리자가 없고, 재기동해도 부트스트랩 계정의 비밀번호는 유지되므로 결국 DB를
  직접 수정하는 수밖에 없었습니다(bcrypt 해시 포함).
  ```
  seccheck admin-recover --username admin --password '새 비밀번호' --clear-totp --unlock --grant-admin
  ```
  - 호스트 셸과 DB 접속 정보가 있어야 실행됩니다 — 그것이 이 명령의 권한 경계입니다.
  - 기존 계정에만 동작하고, IdP 계정의 비밀번호는 거부합니다. 지정한 항목만 바꿉니다.
  - 복구 시 해당 계정의 모든 세션을 종료하고 `RECOVER_ADMIN` 감사 이벤트를 남깁니다.
    권한 계정을 정규 경로 밖에서 되살린 사실은 나중에 보여야 하기 때문입니다.

## v0.90.0

### 개선
- **담당자 없는 정체 심의 알림을 한 통으로 묶습니다.** 직전 버전은 담당자 미배정
  심의를 보안 담당자 전원에게 각각 보냈습니다. 큐가 밀린 설치에서는 심의 50건 ×
  담당자 20명 = 1,000통이 3일마다 나가게 되고, 그런 알림은 아무도 읽지 않습니다.
  이제 담당자가 있는 건은 그 사람에게 개별로, 담당자가 없는 건들은 **건수와 가장
  오래된 대기 일수를 담은 한 통**으로 묶어 보냅니다(오래된 3건은 심의번호까지
  표시). 담당자가 있는 심의의 알림 동작은 그대로입니다.

## v0.89.0

### 추가
- **멈춰 있는 심의를 담당자에게 알립니다.** 지금까지 기한 알림은 모두 요청자 쪽을
  향해 있었습니다(보완 조치·후속조치). 정작 공이 검토자·승인자에게 넘어간 상태 —
  제출됐는데 아무도 잡지 않은 건, 검토를 시작해 놓고 멈춘 건, 결재 대기 상태로
  방치된 건 — 은 아무에게도 알리지 않았고, 요청자는 진행이 없다는 것만 보일 뿐
  밀 방법이 없었습니다. 이제 3일 이상 움직이지 않으면 담당자에게 `심의 정체`
  알림이 가고(담당자 미배정이면 보안 담당자 전원), 같은 건은 3일에 한 번만
  알립니다. 알림에서 심의로 바로 이동할 수 있고, 수신 설정에서 차단할 수 있습니다.
- 마이그레이션 `017`이 마지막 알림 시각(`stalled_reminded_at`)을 기록합니다.

## v0.88.0

### 수정
- **워커가 보내는 알림이 이메일로 나가지 않던 문제를 고쳤습니다.** 보완 조치 기한,
  후속조치 기한, 증적 악성코드 탐지 알림은 백그라운드 워커가 알림 행만 만들고
  발송 작업은 넣지 않았습니다. 그 결과 `하루 한 번 요약` 사용자는 요약에 포함돼
  받았지만, **기본값인 `즉시` 사용자에게는 메일이 한 통도 가지 않았습니다** —
  서비스에 접속해야만 알 수 있었습니다. 기한 알림은 화면을 보고 있지 않은 사람에게
  닿기 위한 것이므로 정반대의 동작이었습니다. 이제 화면에서 발생하는 알림과 동일한
  경로(`Store.Notify`)를 사용해 수신 설정·차단 목록·요약 여부를 그대로 따릅니다.
- 작업 큐 정체·작업 재시도 소진 알림은 의도적으로 인앱 전용을 유지합니다. 이메일
  경로 자체가 멈춘 상황을 알리는 알림이기 때문이며, 코드에 그 이유를 남겼습니다.

## v0.87.0

### 변경
- **보완 요청에도 완료 예정일을 필수로 받습니다.** 후속조치와 같은 이유입니다.
  기한 없이 등록된 보완 요청은 기한 알림(2일 전) 조회에 걸리지 않고, 심의 목록의
  지연 건수·지연 필터에도 잡히지 않습니다. 요청은 한 번 통보되고 그 뒤로는 아무도
  챙기지 않습니다. 이제 기한이 비어 있으면 422(`DUE_DATE_REQUIRED`)로 거절하고,
  화면에서도 `완료 예정일`이 필수 표시로 바뀝니다.

### 주의
- API로 보완 요청을 등록하는 연계 스크립트는 `due_date`를 반드시 채워야 합니다.

## v0.86.0

### 변경
- **후속조치를 적을 때 조치 기한을 필수로 받습니다.** 기한 없이 저장된 후속조치는
  기한 알림(7일 전·초과)의 조회 조건에 걸리지 않고 등록부에서 지연으로도 판정되지
  않습니다. 즉 약속은 기록에 남지만 **아무도 챙기지 않는 상태**가 됩니다. 이제
  후속조치 내용이 있는데 기한이 비어 있으면 422(`FOLLOW_UP_DUE_REQUIRED`)로
  거절하고, 화면에서도 `조치 기한`이 필수 표시로 바뀝니다. 후속조치가 없는 판정은
  종전대로 기한 없이 저장됩니다.
- 이미 기한 없이 저장된 항목은 그대로 유지되며, 후속조치 등록부에서 `기한 없음 ·
  알림이 가지 않습니다`로 구분해 표시합니다.

### 주의
- API로 판정을 기록하는 연계 스크립트가 후속조치만 보내고 기한을 비워 두면 이제
  422로 거절됩니다. 기한을 함께 보내도록 수정해야 합니다.

## v0.85.0

### 개선
- **이미 저장된 잘못된 적용 규칙을 Rule Engine 시뮬레이터가 찾아 줍니다.** 지난
  버전에서 새 규칙은 저장 시점에 막았지만, 그 전에 저장된 규칙은 그대로 남아
  있습니다. 시뮬레이터가 각 항목의 규칙을 함께 검사해, 심의 입력에 없는 이름을
  가리키는 항목을 `적용 규칙 오류` 표로 따로 보여 주고 원인을 적습니다. 이 항목들은
  특정 프로필에서 제외되는 것이 아니라 **어떤 심의에도 배정되지 않으므로**, 제외
  목록에 섞어 두지 않고 별도로 표시합니다. 응답에는 `broken` 개수와 항목별
  `rule_error`가 추가됐습니다.

## v0.84.0

### 개선
- **적용 규칙의 항목명·연산자를 저장 시점에 검사합니다.** 규칙이 심의 입력에 없는
  이름(예: `exposuer`)을 가리키면 그 조건은 영원히 거짓이 되어, 해당 체크리스트
  항목이 **어느 심의에도 배정되지 않습니다**. 아무도 평가하지 않는 보안요건이
  생기는데 경고는 어디에도 뜨지 않았고, 오타 하나면 충분했습니다. 이제 항목 추가·
  수정 시 규칙을 검사해 알 수 없는 항목명, 지원하지 않는 연산자, 비어 있는
  `all`/`any`, 목록이 아닌 `in` 값을 422로 거절하고 어느 부분이 문제인지
  알려 줍니다. 검사에 쓰는 항목 목록은 Rule Engine이 실제로 평가하는 값에서
  그대로 뽑으므로 둘이 어긋날 수 없습니다.
- 이미 저장된 규칙은 그대로 동작합니다. 잘못된 규칙이 있는지 확인하려면 Rule Engine
  시뮬레이터에서 대상 항목이 배정되는지 보십시오.

## v0.83.0

### 개선
- **담당자 선택 목록을 한 번만 불러옵니다.** 항목마다 있는 담당자 선택 상자가 각자
  전체 사용자 목록을 내려받고 있어서, 항목이 100개인 심의를 열면 같은 목록을 100번
  요청했습니다. 이제 한 번 받아 화면 전체가 공유하며, 관리자가 계정을 만들거나
  비활성화하면 다시 불러옵니다.
- **사용자 목록을 검색으로 좁힐 수 있습니다.** `GET /api/v1/users/directory?q=`가
  이름·계정·부서 부분 일치로 필터링합니다. IdP에서 전사 계정을 동기화하는 설치처럼
  활성 사용자가 수천 명인 환경을 위한 것입니다. 비활성 계정은 이전과 같이 제외됩니다.

## v0.82.0

### 추가
- **증적 버전 이력을 열람하고 이전 버전을 내려받을 수 있습니다.** 증적을 새 버전으로
  교체하면 이전 파일은 암호화된 상태로 그대로 보관되고 해시·업로더·시각까지 남아
  있었지만, 그것을 읽을 방법이 없었습니다. 증적이 v3라는 사실만 보이고 v1과 v2가
  무엇이었는지는 확인할 수 없었습니다. 판정의 근거가 되는 자료가 심의 도중 교체된
  경우 검토자가 그 사실과 내용을 확인할 수 있어야 합니다.
  - `GET /api/v1/evidences/{id}/versions`가 버전별 파일명·크기·SHA-256·업로더·시각과
    파기 여부를 돌려줍니다.
  - `GET /api/v1/evidences/{id}/download?version=N`으로 이전 버전을 내려받습니다.
    보존 기간이 지나 파기된 버전은 410으로 거절하되 이력에는 남습니다. 악성코드
    검사 미완료 파일 거부와 무결성 검증은 현재 버전과 동일하게 적용됩니다.
  - 항목 상세의 증적 줄에 `이력` 버튼이 생겼습니다(교체된 적이 있을 때만).
- 마이그레이션 `016`이 버전별 원본 파일명을 보관합니다. 현재 버전은 기존 값으로
  채워지고, 이전에 교체된 버전은 기록이 없었으므로 파일명이 비어 있습니다.

## v0.81.0

### 추가
- **감사로그를 대상으로 좁혀 볼 수 있습니다.** 모든 감사 이벤트는 대상을 기록하고
  화면의 표에도 대상이 보이지만, 정작 "이 Control에 무슨 일이 있었나"를 물어볼
  방법이 없어 전체 로그를 훑어야 했습니다. 이제 표의 대상 값을 누르면 그 대상의
  이력만 남고(`target`·`target_type` 질의 매개변수), 조건이 주소창에 남아 링크로
  공유됩니다. Security Control 목록에는 `이력` 버튼이 생겨 해당 통제의 변경
  이력으로 바로 이동합니다. 조회는 마이그레이션 `008`의 대상 인덱스를 그대로
  사용하므로 로그가 커져도 비용이 늘지 않습니다.

## v0.80.0

### 추가
- **재시도를 모두 소진한 작업을 관리자에게 알립니다.** 지금까지 큐 알림은 "밀려
  있는" 상태만 감지했습니다. 5회 재시도를 모두 소진한 작업은 `FAILED`가 되어
  큐에서 사라지므로, 화면상으로는 큐가 깨끗해 보이는데 알림 메일과 증적 검사는
  조용히 멈춰 있는 상태가 됩니다 — SMTP 비밀번호가 틀렸을 때 관리자 눈에 보이는
  모습이 정확히 이것입니다. 이제 최근 6시간 내에 소진된 작업이 있으면
  시스템 관리자에게 인앱 알림(`JOB_FAILED`, 마지막 오류 메시지 포함)이 가고,
  같은 장애가 이어져도 6시간에 한 번만 알립니다. 알림에서 바로 작업 큐 화면으로
  이동할 수 있으며, 이 유형도 알림 수신 설정에서 차단할 수 있습니다.

## v0.79.0

### 수정
- **알림 유형 차단이 일일 요약 메일에는 적용되지 않던 문제를 고쳤습니다.** 차단은
  즉시 발송만 막고 있었고, 요약 메일은 아직 발송되지 않은 알림을 유형과 무관하게
  모두 모아 보냈습니다. `하루 한 번 요약`을 선택한 사용자는 차단한 유형을
  **하루치로 모아서 다시 받고 있었습니다**. 이제 요약도 차단 목록을 따르며,
  보낼 내용이 전부 차단된 유형이면 빈 요약을 보내지 않습니다. 차단한 알림은
  화면의 알림 목록에는 그대로 남습니다.

## v0.78.0

### 수정
- **등록부에 표시되는 날짜와 CSV 내보내기의 시각이 표시 시간대를 따르도록
  고쳤습니다.** 시간대 정리의 마지막 지점입니다.
  - 후속조치 등록부의 `판정일`, `보고일`, `이행 확인일`은 시각을 날짜로 바꿔
    보여주는데, 이 변환이 UTC 기준이라 표시 시간대 오전 9시 이전에 처리한 건이
    화면에는 하루 전으로 표시됐습니다. 새 마이그레이션 `015`의 `display_date()`가
    표시 시간대로 변환합니다.
  - 감사로그·심의 목록 CSV의 시각이 UTC(`...T15:30:00Z`)로 적혀 있어, 화면과
    대조하려면 9시간을 더해야 했습니다. 이제 표시 시간대 기준
    `2026-08-23 00:30:00` 형식으로 적습니다. Excel이 날짜·시각으로 바로 인식합니다.
    XLSX·PDF 내보내기는 이미 표시 시간대를 쓰고 있었습니다.

## v0.77.0

### 수정
- **기간 필터의 하루 시작 시각이 표시 시간대를 따르도록 고쳤습니다.** 심의 목록,
  감사로그, 기간 리포트의 `from`/`to`가 날짜를 UTC 자정으로 해석하고 있었습니다.
  기본 표시 시간대가 `Asia/Seoul`이면 하루가 9시간 늦게 시작하는 셈이라, 새벽
  1시에 올라온 신청은 전날로 집계되고 **월초 리포트에서는 1일 오전 9시 이전 건이
  통째로 빠졌습니다**. 새 마이그레이션 `014`의 `display_day_start()`가 표시 시간대
  기준으로 하루의 시작을 계산하며, 서머타임이 있는 시간대도 날짜별로 정확합니다.
  `display_today()`와 같은 방식으로, 시간대가 비었거나 해석할 수 없으면 UTC로
  물러납니다.

### 주의
- 배포 후 기간 리포트의 집계값이 이전 결과와 조금 달라질 수 있습니다. 경계에
  걸쳐 잘못 분류돼 있던 건이 제 날짜로 이동한 것입니다.

## v0.76.0

### 수정
- **기한 지남(지연)·임박 판정이 표시 시간대를 따르도록 고쳤습니다.** 후속조치 지연
  표시, 보완 조치 기한 임박, 오픈 예정 2주 목록, 기한 알림 메일이 모두
  `current_date`로 판단하고 있었는데, 이는 컨테이너의 UTC 날짜입니다. 기본
  표시 시간대가 `Asia/Seoul`이므로 **매일 자정부터 오전 9시까지는 전날 날짜로
  판단**했습니다. 자정에 기한이 끝난 후속조치가 오전 내내 정상으로 표시되고,
  기한 알림도 하루 늦게 나갔습니다. 새 마이그레이션 `013`이 `display_today()`
  함수를 만들고, 날짜를 비교하는 모든 지점이 이 함수를 사용합니다. 시간대 설정이
  비었거나 해석할 수 없으면 UTC로 물러나며, 쿼리가 실패하지는 않습니다.

### 주의
- 마이그레이션 `013`은 함수 하나를 만들 뿐 테이블을 잠그지 않습니다. 배포 후
  후속조치 등록부에서 지연 건수가 늘어 보일 수 있는데, 이는 그동안 지연으로
  집계되지 않던 건이 제자리를 찾은 것입니다.

## v0.75.0

### 수정
- **일일 요약 메일이 같은 날 두 번 나가던 문제를 고쳤습니다.** 요약을 이미 보냈는지
  판단할 때 컨테이너의 UTC 자정을 기준으로 삼고 있어서, `Asia/Seoul`처럼 UTC보다
  앞선 시간대에서는 UTC 자정(현지 09:00)을 지나는 순간 같은 내용이 한 번 더
  발송됐습니다. 이제 `표시 시간대` 기준 자정으로 하루를 셉니다. 발송 시각
  판단(`digest_hour`)은 이미 표시 시간대를 쓰고 있었고, 이 기준만 어긋나 있었습니다.
- **일부만 보내는 수정 요청이 나머지 값을 지우던 엔드포인트를 정리했습니다.** 지난
  버전에서 고친 체크리스트 항목과 같은 문제가 세 곳에 더 있었습니다.
  - `PATCH /api/v1/me`: 부서만 보내면 표시 이름과 이메일이 지워졌습니다. 이메일이
    비면 알림 메일이 끊깁니다. 표시 이름을 빈 값으로 보내면 422로 거부합니다.
  - `PATCH /api/v1/security-controls/{id}`: 제목만 바꾸면 설명과 담당자가
    지워졌습니다.
  - `PATCH /api/v1/templates/{id}`: 설명이 지워지는 것에 더해, `active`를 보내지
    않으면 항상 `true`로 취급해 **사용 중지된 템플릿이 이름만 바꿔도 다시 노출**
    됐습니다. 이제 보내지 않은 필드는 그대로 둡니다.
  화면은 항상 전체 값을 보내므로 화면 동작은 달라지지 않습니다.

## v0.74.0

### 보안
- **CSV 내보내기가 Excel 수식으로 실행되지 않도록 했습니다.** 심의 이름·부서·표시
  이름처럼 사용자가 입력한 값이 `=`, `+`, `-`, `@`로 시작하면, 감사로그와 심의 목록
  CSV를 내려받아 Excel이나 LibreOffice로 여는 관리자의 PC에서 그 값이 수식으로
  실행됩니다(`=cmd|...`, `=WEBSERVICE(...)`처럼 외부 호출까지 가능합니다). 내보내는
  쪽에서 해당 셀 앞에 작은따옴표를 붙여 텍스트로 표시합니다. Excel 화면에는 보이지
  않으며, 숫자 값과 일반 문구는 그대로입니다. XLSX·PDF·ZIP 내보내기는 값을 수식으로
  해석하지 않으므로 영향이 없습니다.

### 빌드
- **릴리즈 버전이 다섯 파일에 모두 반영됐는지 확인하는 테스트를 추가했습니다.**
  손으로 올리다 한 곳을 빠뜨리면 이전 태그로 이미지가 만들어지거나, README가
  존재하지 않는 태그를 안내하게 됩니다. 이제 `VERSION`과 compose·README·CI·
  package.json의 버전이 어긋나면 CI에서 실패합니다.

## v0.73.0

### 수정
- **초안 항목을 일부만 수정하면 나머지 값이 지워지던 문제를 고쳤습니다.** 항목
  수정(`PATCH .../items/{itemID}`)이 본문에 담긴 값으로 모든 열을 덮어썼기 때문에,
  제목만 보내면 항목 코드·분류·중요도·답변 유형·필수 여부가 함께 빈 값이 되었습니다.
  화면은 항상 전체 항목을 보내므로 드러나지 않았지만, API로 항목을 손보는 경우
  기준 항목이 조용히 망가지는 경로였습니다. 이제 본문에 없는 필드는 그대로 두고,
  보낸 필드만 반영합니다. 항목 코드·제목·질문을 빈 값으로 보내면 422로 거부합니다.
  (다른 PATCH 엔드포인트는 이미 이 방식이었습니다.)

### 감사
- **체크리스트 항목의 추가·수정·삭제를 구분해 기록합니다.** 셋 모두
  `UPDATE_TEMPLATE`로 남아 있어, 감사 로그만 보고는 항목이 새로 생긴 것인지
  지워진 것인지 알 수 없었습니다. 이제 `CREATE_CHECKLIST_ITEM`,
  `UPDATE_CHECKLIST_ITEM`, `DELETE_CHECKLIST_ITEM`으로 나뉘며, 삭제 기록은 사라진
  항목의 코드를 함께 남깁니다. 이전 기록은 그대로 유지되고 화면에서도 종전 이름으로
  보입니다.

### 빌드
- **CI에 고정된 GitHub Action 다이제스트의 버전 주석을 바로잡았습니다.** 릴리즈마다
  버전 문자열을 일괄 치환하면서 `# v0.xx.x` 주석까지 SecCheck 버전으로 덮어써,
  다이제스트를 검증하려는 사람이 존재하지 않는 상위 릴리즈를 찾게 되어 있었습니다.
  실제 태그(trivy-action v0.36.0, sbom-action v0.24.0, action-baseline v0.15.0)로
  되돌리고, 같은 실수가 반복되면 실패하는 테스트를 추가했습니다. 실행되는
  다이제스트 자체는 바뀌지 않았습니다.

## v0.72.0

### 보안
- **삭제된 대상을 가리키는 감사 기록이 해석 불가능하던 문제를 고쳤습니다.** 감사
  이벤트가 대상 ID만 남기는 경우, 그 대상이 삭제되는 종류라면 나중에 무엇이었는지
  알 방법이 없습니다. 기록은 남았는데 답은 못 하는 상태입니다.
  - **Security Control 삭제**: 행이 완전히 지워지므로 코드와 이름이 어디에도 남지
    않았습니다. 이제 삭제 시점의 코드·이름을 함께 기록합니다.
  - **세션 종료**: 세션 행이 지워져 ID가 가리킬 곳이 없었습니다. 종료된 세션의
    브라우저·IP·마지막 활동 시각을 기록합니다.
  - **API 키 폐기**: 어떤 키였는지가 접근 검토의 핵심 질문인데 ID뿐이었습니다.
    키 이름과 접두사를 기록합니다.
  - **비밀번호 재설정·계정 잠금 해제**: 관리자가 누구의 계정에 조치했는지 ID로만
    남아 있었습니다. 대상 계정명을 함께 기록합니다.
- 나머지 항목(로그아웃, 본인 비밀번호 변경, TOTP 등록·해제 등)은 대상 자체가 곧
  내용이므로 그대로 두었습니다.

## v0.71.0

### 보안
- **API 키 발급이 감사로그에 `회전`으로 기록되던 문제를 고쳤습니다.** 발급과 회전이
  같은 함수를 공유하면서 새 키를 만들어도 `API 키 회전` 이벤트가 남았습니다. 감사로그만
  보고서는 **자격 증명이 새로 발급된 것인지 기존 것을 교체한 것인지 구분할 수
  없었습니다.** 접근 검토에서 정확히 그것을 묻는데도 말입니다. 이제 발급은
  `API 키 발급`, 회전은 `API 키 회전`으로 각각 기록되고, 회전에는 교체된 키 ID가
  남습니다.
- **OIDC Discovery 연결 테스트도 감사로그에 남깁니다.** SMTP 테스트는 처음부터
  기록하고 있었는데 OIDC는 빠져 있었습니다. 둘 다 관리자가 지정한 주소로 서버가
  외부 요청을 보내는 동작이며, 폐쇄망 설치본에서는 특히 남길 만한 기록입니다.

### 개선
- 상태를 바꾸면서 감사 이벤트를 남기지 않는 엔드포인트를 전수 조사했습니다. 나머지는
  읽기 전용(가져오기 미리보기, Rule 시뮬레이션, MCP 조회)이거나 본인 알림 읽음 처리로
  기록할 필요가 없는 것들이었습니다.

## v0.70.0

### 보안
- **증적 파기를 감사 체인에 기록합니다.** 보존 기간이 지난 증적의 암호문을 파기하는
  일은 **서버 로그에만, 그것도 건수만** 남고 있었습니다. 그런데 서버 로그는 같은
  보존 정책으로 삭제되므로, **파기 기록이 파기 대상과 함께 사라집니다.** 유일한
  사본을 없애는 작업의 근거가 보존 기간이 지나면 남지 않는 셈입니다.
- 이제 파일마다 `증적 보존기간 만료 파기` 감사 이벤트를 남깁니다(파일명, 버전,
  SHA-256, 대상 심의, 적용된 보존 일수). 감사로그는 보존 정책의 삭제 대상이 아니므로
  "이 증적이 언제 어떤 정책으로 파기되었는가"에 답할 수 있습니다.
- 같은 파일을 두 번 기록하지 않습니다.

## v0.69.0

### 문서
- **여러 버전을 건너뛰어 올라올 때 확인할 것을 한 표로 모았습니다.** 변경 이력이 69개
  릴리즈 1,000줄을 넘어, 오래된 설치본을 올리는 운영자가 자기에게 해당하는 주의사항을
  찾으려면 전체를 훑어야 했습니다. 오프라인 배포는 업그레이드 간격이 길어 여러
  릴리즈를 한 번에 건너뛰는 것이 보통입니다.
- 동작이 달라졌거나 배포 전 확인이 필요한 항목만 버전별로 정리했습니다: 프록시 뒤
  클라이언트 IP(`v0.2.0`), 로그인 제한 의미 변경(`v0.3.0`), ClamAV 비동기 전환
  (`v0.4.0`), `pg_trgm` 권한(`v0.5.0`), 두 건의 API 응답 형식 변경(`v0.10.0`,
  `v0.15.0`), Excel 매핑 기본값(`v0.30.0`), OIDC 역할 재조정(`v0.33.0`),
  `/metrics` 공개 여부(`v0.38.0`), 후속조치 마이그레이션(`v0.59.0~`).
- 마이그레이션 001~012가 각각 무엇을 바꾸는지도 함께 실었고, 새 마이그레이션이 이
  목록에 없으면 CI가 실패합니다.

## v0.68.0

### 기능
- **대시보드에 `내 후속조치`가 추가되었습니다.** 미조치 항목 등록부는 심의 리포트에
  있고 리포트는 보안 담당자·감사자용이라 **요청자는 열 수 없습니다.** 정작 조치를
  하는 쪽이 자기 몫을 한 곳에서 볼 방법이 없어 심의를 하나씩 열어 봐야 했습니다.
- 이행해야 할 후속조치를 심의를 가로질러 모아 보여 주며, 기한 초과와 `확인 대기`
  상태를 구분합니다. 접근 권한은 기존 심의 접근 규칙을 그대로 따르므로 남의 심의
  조치는 보이지 않습니다.

## v0.67.0

### 기능
- **후속조치 보고와 확인에 알림이 붙었습니다.** 직전 릴리즈에서 조치 담당자가 완료를
  보고할 수 있게 만들었지만, 보고해도 아무에게도 통보되지 않아 **누군가 리포트를 열
  때까지 확인되지 않은 채로 남았습니다.** 기한 알림을 추가하며 없앤 것과 같은 종류의
  침묵입니다.
- 이제 보고하면 검토자에게 `후속조치 이행 보고` 알림이, 확인되면 보고한 사람에게
  `후속조치 이행 확인` 알림이 갑니다. 두 알림 모두 알림 수신 설정에서 개별로 끌 수
  있습니다.

## v0.66.0

### 기능
- **후속조치를 실제로 수행한 쪽이 완료를 보고할 수 있습니다.** v0.59.0에서 등록부를
  종료할 수 있게 만들면서 그 권한을 보안 담당자에게만 뒀는데, **정작 조치는 서비스
  담당 쪽에서 합니다.** 완료했다는 사실을 시스템 밖에서 전달해야 했습니다.
- 보완 요청이 오래전부터 쓰던 방식(보고 → 확인)에 맞췄습니다. 항목 상세의
  `조치 완료 보고`로 조치 내용을 남기면 `조치 보고됨` 상태가 되고, 보안 담당자가
  리포트에서 `이행 확인`을 해야 등록부에서 빠집니다. **보고만으로는 종료되지 않으므로
  등록부는 여전히 보안팀의 기록입니다.**
- 보고와 확인 모두 감사로그(`조치 사항 이행 보고`, `조치 사항 이행 완료`)에 남고,
  리포트와 Excel에 보고일·보고자·확인일·확인자가 함께 표시됩니다.
- 기존 `{"done": true}` 형식의 호출은 그대로 이행 확인으로 동작합니다.

### 설정
- 마이그레이션 `012`가 `review_results`에 보고자 관련 컬럼과 인덱스를 추가합니다.

## v0.65.0

### 수정
- **기한 알림이 퇴사자 계정으로 발송되던 문제를 고쳤습니다.** 보완 요청과 후속조치의
  기한 알림은 받는 사람이 활성 계정인지 확인하지 않았습니다. 이 기한들은 심의가 끝나고
  몇 달 뒤에 돌아오는데, **그 시점이 바로 담당자가 조직에 없을 가능성이 가장 높은
  때입니다.** 비활성 계정으로 간 알림은 아무도 읽지 않으면서 조치가 진행 중인 것처럼
  보이게 만듭니다. 보완 요청의 경우 미완료 상태가 재제출을 막으므로 심의 자체가
  멈춥니다.
- 이제 원 담당자가 비활성이면 심의 요청자, 그다음 검토자 순으로 전달하고, 전달된
  알림에는 원 담당자가 비활성이라는 사실과 재지정 요청을 함께 표시합니다. 받을 사람이
  아무도 없으면 발송하지 않고 서버 로그에 WARN으로 남깁니다.

## v0.64.0

### 수정
- **심의가 언제 이루어졌는지 화면에 없던 문제를 고쳤습니다.** 제출일·최종 제출일·승인일은
  내보낸 파일에만 있었고 화면에는 상태 배지뿐이었습니다. 감사자가 화면으로 심의를
  확인할 때 "언제"를 답할 수 없었습니다. 심의 상단 `진행 경과`에 표시합니다.
- **증적에 누가 첨부했는지 표시합니다.** 파일명·크기·버전·검사 상태는 보여 주면서
  업로더는 빠져 있었습니다. 여러 사람이 함께 작성하는 심의에서 누가 무엇을 냈는지
  확인할 수 없었습니다.

### 개선
- 스키마에 저장되지만 화면 어디에도 나타나지 않는 항목을 전수 조사했습니다. 20건 중
  대부분은 내부 처리용이었고, 위 두 가지가 실제로 사용자에게 필요한데 빠져 있었습니다.

## v0.63.0

### 수정
- **심의의 최종 결과와 최종 의견이 화면에 없던 문제를 고쳤습니다.** 검토자가 검토를
  완료하며 남기는 `최종 의견`, 승인자가 남기는 결재 코멘트는 저장만 되고 어디에도
  표시되지 않았습니다. **반려된 심의를 받은 요청자는 왜 반려됐는지 화면에서 알 수
  없었고**, 결과물을 내려받아야 확인할 수 있었습니다.
- 심의 상단에 `심의 결론`이 추가되어 최종 결과, 최종 의견, 승인·반려 코멘트와 결재자,
  일시를 표시합니다.
- 결재 이력(`approvals`)이 심의 상세 응답에 포함됩니다. 기존 최종 결과·의견은 이미
  내려가고 있었고 화면만 없었습니다.

## v0.62.0

### 수정
- **검토 결과를 요청자가 볼 수 없던 문제를 고쳤습니다.** 보안 담당자의 판정, 검토
  의견, 요청한 후속조치는 **검토자가 편집하는 패널 안에만** 그려지고 있었습니다. 그
  패널은 `검토 중` 상태의 배정된 검토자에게만 보이므로, 정작 그 의견이 향하는
  요청자는 자기 항목이 왜 그렇게 판정되었는지 화면 어디에서도 볼 수 없었고, 검토가
  끝나면 검토자 본인도 못 봤습니다.
- 직전 릴리즈에서 추가한 후속조치 기한 알림이 이 문제를 두드러지게 만들었습니다.
  알림을 눌러 심의로 들어가도 요청받은 조치가 화면에 없었습니다.
- 이제 판정이 있는 항목은 검토 결과·최종 적용 여부·검토 의견·후속조치·기한·이행
  여부를 읽기 전용으로 표시합니다. 데이터는 원래 API가 내려주고 있었고 화면만
  없었습니다.

## v0.61.0

### 기능
- **후속조치 기한 알림을 발송합니다.** 기한을 넣을 수 있게 됐지만 아무도 통보받지
  않아, 리포트를 열어야만 알 수 있었습니다. 정작 그 기한은 심의가 끝난 뒤 몇 달 지나
  돌아오고, 그 사이에 리포트를 여는 사람은 없습니다. 이제 기한 7일 전부터, 그리고
  기한이 지난 뒤에 알림이 발송됩니다.
- **수신자는 심의 요청자입니다.** 후속조치는 그 서비스에 붙은 조건이고 실제 조치는
  요청자 쪽에서 합니다. 보안팀은 리포트의 등록부로 전체를 봅니다.
- 같은 항목은 주 1회까지만 알립니다. 이행 완료된 항목, 기한이 없는 항목, 아직 기한이
  많이 남은 항목은 알리지 않습니다.
- 알림 수신 설정에 `후속조치 기한` 항목이 추가되었습니다.

### 설정
- 마이그레이션 `011`이 `review_results.follow_up_reminded_at`을 추가합니다.

## v0.60.0

### 기능
- **후속조치에 기한을 지정할 수 있습니다.** 기한 없는 약속은 추적되지 않습니다.
  "3개월 내 보완"이라고 적어도 시스템은 언제까지인지 알 수 없었고, 등록부는 어떤
  항목이 늦었는지 말할 수 없었습니다. 보완 요청은 처음부터 기한이 있었는데, 심의보다
  오래 남는 판정의 후속조치에는 없었습니다.
- 항목 판정 화면에 `조치 기한`이 추가되었고, 리포트의 미조치 항목은 기한이 지난 건을
  `기한 초과`로 표시하며 기한이 이른 순으로 정렬합니다. Excel 리포트에도 기한과 초과
  여부가 함께 나갑니다.

### 설정
- 마이그레이션 `010`이 `review_results.follow_up_due_date`와 조회용 인덱스를
  추가합니다. 기존 후속조치는 기한 없음으로 남으며, 기한 없는 항목은 초과로 표시되지
  않습니다.

다음 단계로 기한이 임박하거나 지난 후속조치에 대한 알림이 남아 있습니다. 현재는
리포트를 열어야 확인할 수 있습니다.

## v0.59.0

### 기능
- **미조치 항목을 이행 완료 처리할 수 있습니다.** 직전 릴리즈의 집계는 약속을 모아
  보여 주기만 해서 목록이 늘어나기만 했고, 작년 항목이 이행된 것인지 잊힌 것인지
  구분할 수 없었습니다. 이제 `조치 완료`로 이행 결과를 남기고 종료하며, 종료된 항목은
  기본 목록에서 빠지고 `이행 완료 포함`으로 다시 볼 수 있습니다. 잘못 종료했다면
  `해제`로 되돌립니다.
- 이행과 해제는 모두 감사로그(`조치 사항 이행 완료`, `조치 사항 이행 완료 해제`)에
  남고, Excel 리포트에 이행일·이행자·이행 결과가 함께 나갑니다.
- **원 검토자가 아니어도 보안 담당자면 처리할 수 있습니다.** 조치 기한이 돌아올 때쯤
  그 항목을 판정한 사람이 남아 있으리라는 보장이 없기 때문입니다.

### 설정
- 마이그레이션 `009`가 `review_results`에 이행 완료 관련 컬럼과 인덱스를 추가합니다.
  기존 데이터는 모두 미조치 상태로 시작합니다.

## v0.58.0

### 기능
- **심의 리포트에 `미조치 항목` 집계가 추가되었습니다.** 검토자가 판정과 함께 남기는
  `조치 사항`은 지금까지 그 항목을 만든 심의 안에서만 보였습니다. 조건부 적합으로
  통과시키면서 "3개월 내 보완" 같은 약속을 남겨도, 심의가 끝나면 그 약속들이 여러
  심의에 흩어져 팀 전체의 미이행 목록을 볼 방법이 없었습니다. 검토가 끝난 뒤 남는
  일이 바로 그것인데도 제품에서 사라졌습니다.
- 심의번호·서비스·부서·항목·판정·조치 내용·판정일을 최근 판정순으로 모아 보여 주며,
  기간과 부서 필터가 그대로 적용되고 Excel 리포트에도 `미조치 항목` 시트로 포함됩니다.
- 조치 사항이 비어 있는 판정은 집계에 들어가지 않습니다.

## v0.57.0

### 테스트
- **증적 암호화와 키 관리를 검토하고 미검증 속성에 테스트를 붙였습니다.** 구현은
  정확했고 고칠 것은 없었습니다. 다만 설계가 명시적으로 주장하는 속성 중 검증되지
  않은 것들이 있었습니다.
  - **다른 파일에서 잘라 붙인 청크**: 같은 사용자의 두 증적은 같은 키를 쓰므로, 이를
    막는 것은 추가 인증 데이터에 담긴 증적 ID뿐입니다. 이제 실제로 잘라 붙여
    거부되는지 확인합니다.
  - **같은 파일 내 청크 중복**: 청크는 위치에 묶여 있으므로 자기 파일의 청크라도
    다른 위치에서는 열리지 않아야 합니다.
  - **마지막 청크 뒤에 덧붙인 바이트**: 마지막 청크는 항상 읽기 버퍼보다 짧으므로
    덧붙인 바이트가 그 청크에 흡수되어 인증에 실패합니다. 구조적으로 탐지됩니다.
  - **감싼 키를 다른 사용자 행으로 옮기기**: 개인 키는 사용자와 버전을 담은 추가
    인증 데이터로 감싸므로, DB 수준의 행 교체나 잘못된 복구로도 남의 키를 쓸 수
    없습니다. `internal/vault`에는 테스트가 하나도 없었습니다.
  - **증적과 레코드의 결합**: 같은 바이트라도 다른 증적 ID나 다른 버전으로는 열리지
    않습니다.

## v0.56.0

### 문서
- **규모별 성능 측정값을 운영 가이드에 실었습니다.** "이벤트가 수백만 건 쌓여도
  일상 점검 시간이 늘어나지 않는다"고 적어 두었지만 숫자는 없었습니다. 심의 5,000건과
  감사 이벤트 20만 건을 적재해 측정한 결과, 대시보드·심의 목록(검색과 깊은 페이지
  포함)·감사로그 조회(필터 포함)·기간 리포트·`/metrics`가 모두 15ms 이내였고,
  감사로그 CSV 5만 행 내보내기는 약 0.23초였습니다. 인덱스 문제는 없었습니다.
- 해시 체인 비용도 실제 체인으로 측정했습니다: 전체 재검증은 건당 약 6μs(3,001건에
  17ms, 100만 건이면 약 6초), 증분 검증은 신규분만 확인합니다. **감사 이벤트 기록은
  초당 약 440건이며, 체인 특성상 모든 감사 대상 작업이 이 지점에서 직렬화됩니다.**

### 테스트
- 증분 검증이 실제로 검증 지점을 사용하는지 확인합니다. 전체 재검증 직후 증분 검증이
  전체를 다시 훑으면 실패하므로, 검증 지점 기록이 깨지면 CI가 잡습니다.

## v0.55.0

### 테스트
- **승인 완료된 심의의 Excel 결과물 내용을 검증합니다.** 기존 검증은 작성 중인 심의의
  증적 시트만 확인했고, 정작 조직이 보관하고 감사에 제출하는 **완료본**은 응답 코드와
  파일 크기만 보고 있었습니다. 이제 생애주기 테스트가 승인 직후 워크북을 열어 심의번호,
  서비스명, 최종 결과, 최종 의견, 항목별 검토 결과가 실제로 담겨 있는지 확인합니다.
- 검증이 헛돌지 않는지 확인하기 위해 존재하지 않는 문자열로 실패를 재현했습니다.

## v0.54.0

### 테스트
- **반려와 종료 경로를 검증합니다.** 지난 릴리즈가 승인까지 가는 길을 덮었다면, 이번엔
  나머지 절반입니다. 승인 절차가 꺼져 있으면 검토자의 반려가 그대로 최종 결과가 되고,
  켜져 있으면 검토자가 적합으로 넘긴 건을 승인자가 반려할 수 있습니다. 이 경우
  검토자의 `APPROVED` 판정이 `REJECTED`로 덮이는지, 결재 기록이 남는지, 배정된
  검토자만 심의를 종료할 수 있는지까지 확인합니다.

### 문서
- **반려할 때에도 모든 항목에 판정이 필요하다는 점을 명시했습니다.** 서버는 미검토
  항목이 있으면 검토 완료를 거부하는데(`REVIEW_INCOMPLETE`), 사용자 가이드에는
  "모든 항목 검토가 끝나면"이라고만 적혀 있어 반려에도 적용되는지 알 수 없었습니다.
  반려가 무엇을 확인했는지에 대한 기록으로 남아야 하므로 의도된 동작이며, 반복되는
  판정은 `일괄 판정`으로 처리하면 됩니다. 미완료 보완 요청이 남아 있어도 완료할 수
  없다는 점도 함께 적었습니다.

## v0.53.0

### 테스트
- **심의 생애주기 전체를 처음으로 한 번에 검증합니다.** 개별 단계에는 테스트가
  있었지만 이들을 잇는 경로는 없었고, `begin-review`와 `complete-review`는 테스트에서
  **한 번도 호출된 적이 없었습니다.** 이 제품이 존재하는 이유 자체가 검증되지 않고
  있었던 셈입니다.
- 서로 다른 네 사람(요청자·검토자·승인자·관리자)이 실제 순서대로 진행합니다: 심의
  생성과 자동 배정 → 작성 → 제출 → 검토 시작 → 일괄 판정 → 보완 요청으로 반려 →
  보완 조치와 검증 → 재제출 → 검토 완료 → 최종 승인. 각 단계마다 상태 전이를
  확인하고, 마지막에 결과물 내보내기·결재 기록·판정 기록·**감사 체인 전체 재검증**까지
  확인합니다.
- 결함은 발견되지 않았습니다. 전 구간이 정상 동작하며, 이제 회귀가 생기면 CI가
  잡습니다.

## v0.52.0

### 테스트
- **동시 실행 동작을 실제 데이터베이스로 검증했습니다.** 심의 번호는 `count+1`을
  advisory 락 안에서 계산하는데, 락이 없으면 동시 생성 시 같은 번호를 계산해 하나가
  unique 제약으로 실패합니다. 바쁜 시간대에만 드러날 종류의 문제라 8건 동시 생성으로
  번호가 모두 달라지는 것을 고정했습니다. 결과는 정상이었고, 락을 단순화하려는
  변경이 있으면 이제 CI가 잡습니다.
- `expected_updated_at`을 보내지 않으면 마지막 쓰기가 이깁니다. 폼을 편집하지 않는
  연계 클라이언트를 위한 계약이며, 문서화된 동작임을 테스트로 고정했습니다. 버전을
  보내는 경우의 충돌 감지는 기존 테스트가 다룹니다.

### 수정
- 증적 검사 결과를 `evidences`와 `evidence_versions`에 한 트랜잭션으로 기록합니다.
  격리 경로는 이미 그렇게 하고 있었고 조회 경로는 `evidences`만 보므로 동작 차이는
  없지만, 서로 어긋난 이력은 나중에 읽는 사람을 오도합니다.
- 악성코드 격리 후 업로더 알림 등록에 실패하면 서버 로그에 남깁니다. 파일은 이미
  체크리스트에서 사라졌는데 본인은 이유를 모르는 상태가 조용히 만들어질 수 있었습니다.

## v0.51.0

### 수정
- **일일 요약 메일 발송 중 도착한 알림이 사라지던 문제를 고쳤습니다.** 요약을 보낸 뒤
  "아직 발송되지 않은 알림 전부"를 발송 완료로 표시하고 있었습니다. 메일을 만들고
  보내는 사이에 새 알림이 도착하면, 그 알림은 **요약에 담기지도 않은 채 발송 완료로
  표시되어** 이메일로는 영영 전달되지 않았습니다. 장애 없이도 발생하는 경로입니다.
  이제 실제로 담은 알림만 표시하므로 그 사이 도착분은 다음 요약에 포함됩니다.
- 발송 후 표시에 실패하면 서버 로그에 남깁니다. 즉시 발송분은 표시가 누락되면 같은
  알림이 요약에 다시 들어가고, 요약분은 다음 주기에 전체가 재발송되므로 중복 메일의
  원인이 됩니다.

## v0.50.0

### 수정
- **제출과 최종 승인이 두 개의 쓰기로 나뉘어 있고 두 번째가 실패해도 무시되던 문제를
  고쳤습니다.** 두 경우 모두 심의 상태는 먼저 확정되고, 짝이 되는 기록은 그 뒤에
  별도로 쓰이면서 오류가 버려졌습니다.
  - **제출**: 심의는 `SUBMITTED`인데 제출 레코드는 `DRAFT`로 남고 제출자와 제출
    시각이 비는 상태가 될 수 있었습니다. 처리 기간 리포트가 이 시각을 기준으로
    계산하므로 집계까지 조용히 틀어집니다.
  - **최종 승인**: 심의는 `APPROVED`인데 **누가 승인했는지에 대한 기록이 없는**
    상태가 될 수 있었습니다.
- 이제 각각 한 트랜잭션으로 처리되어 둘 다 남거나 둘 다 남지 않습니다. 실패하면
  상태가 그대로 유지되고 사용자는 다시 시도할 수 있습니다.

## v0.49.0

### 수정
- **증적과 악성코드 검사 작업을 같은 트랜잭션에 기록합니다.** 검사 작업은 증적을
  저장한 뒤 별도 문장으로 넣고 있었고 그 오류는 버려졌습니다. 작업이 유실되면 증적은
  영원히 `PENDING`으로 남아 **다운로드도 제출도 막히는데, 큐에는 재시도할 작업조차
  없어** 관리자가 손쓸 방법이 없었습니다. 증상은 `검사 대기 증적` 수가 줄지 않는 것
  하나뿐이었습니다.
- 이제 둘 중 하나만 남는 일이 없습니다. 큐에 넣지 못하면 업로드가 실패하고 저장된
  암호문도 삭제되므로, 사용자는 즉시 다시 시도할 수 있습니다.

## v0.48.0

### 보안
- **감사 이벤트 기록 실패가 조용히 사라지던 문제를 고쳤습니다.** 161개 호출 지점이
  모두 반환된 오류를 버리고 있었습니다. 호출자가 할 수 있는 일이 없기 때문입니다 —
  기록하려던 행위는 이미 일어났으므로 요청을 실패시키는 것은 거짓말입니다. 문제는
  **체인 검증으로도 잡히지 않는다**는 점입니다. 남은 이벤트끼리는 일관되므로 체인은
  정상으로 나오고, 빠진 이벤트가 있다는 사실은 어디에도 드러나지 않습니다. 위변조
  방지 기록을 표방하는 서비스에서 가장 나쁜 실패 방식입니다.
- 이제 실패한 이벤트는 세 곳에 남습니다: `seccheck_audit_write_failures` 지표,
  `component=audit` ERROR 로그, 그리고 데이터베이스가 문제인 경우를 위한 컨테이너
  표준 오류.

### 설정
- 지표 `seccheck_audit_write_failures`가 추가되었습니다. **0이 아니면 기록 없이
  수행된 행위가 있다는 뜻이므로 즉시 조사해야 합니다.** 프로세스 메모리에 있으므로
  재시작하면 0으로 돌아갑니다 — 데이터베이스가 불안정할 때 늘어나는 값을
  데이터베이스에 저장할 수는 없기 때문입니다.

## v0.47.0

### 수정
- **500 오류의 원인이 어디에도 남지 않던 문제를 고쳤습니다.** 서버가 500을 응답하는
  97개 지점 중 원인을 기록하는 곳이 하나도 없었습니다. 접근 로그에는 "이 요청이
  실패했다"만 남고 왜인지는 사라져서, 인터넷이 닿지 않는 설치본에서는 손쓸 방법이
  없었습니다. 이제 `component=api` 로그에 코드·경로·원인이 함께 남고 요청 ID로 접근
  로그와 대조할 수 있습니다.
- **데이터베이스 장애가 자기 기록을 지우던 문제를 고쳤습니다.** 구조화 로그는 DB에
  저장되므로, 정작 DB가 문제일 때 그 사실을 기록할 수 없었고 오류는 조용히 버려졌습니다.
  이제 저장에 실패하면 해당 줄이 컨테이너 표준 오류로 출력됩니다.
- **심의 생성·복사·체크리스트 배정 실패 시 원본 데이터베이스 오류를 응답에 담아
  보내던 것을 중단했습니다.** 테이블·컬럼 이름이 호출자에게 노출됐습니다. 원인은
  이제 로그에만 남습니다. 템플릿 가져오기는 대상이 체크리스트 관리자이고 사유가
  실제로 도움이 되므로 화면 표시를 유지하되 로그에도 남깁니다.
- 한글 글꼴 누락으로 PDF 생성이 실패하면 서버 로그에도 남습니다. 패키징 문제를
  내보내기를 시도한 사용자만 알게 되는 상황을 피하기 위한 것입니다.

## v0.46.0

### 기능
- **증적을 여러 건 한 번에 첨부할 수 있습니다.** 하나의 보안요건을 화면 캡처 여러 장으로
  증명하는 일이 흔한데, 파일을 하나 고르고 업로드하고 기다리기를 반복해야 했습니다.
  이제 파일 선택 창에서 여러 개를 한 번에 지정하면 순서대로 저장됩니다.
- 각 파일은 기존과 같이 별도 증적으로 기록됩니다. 일부가 확장자 정책이나 크기 제한에
  걸려 실패해도 **이미 저장된 것은 그대로 남고** 실패한 파일만 사유가 표시됩니다.

## v0.45.0

### 기능
- **`selftest --full`이 만든 심의를 다시 취소합니다.** 검증 환경에 반복 실행하면 초안이
  계속 쌓였습니다. 이제 내보내기까지 확인한 뒤 취소하므로 환경이 지저분해지지 않고,
  취소 경로 자체도 함께 검증됩니다. 삭제가 아니라 취소이므로 기록은 남습니다.

이번 릴리즈는 서비스 동작 변경이 없습니다.

## v0.44.0

### 기능
- **릴리즈가 자기 image를 직접 검증합니다.** 지난 릴리즈에서 추가한 `selftest`를
  릴리즈 워크플로에 넣었습니다. 빌드된 컨테이너를 빈 데이터베이스로 띄워
  마이그레이션, 기본 체크리스트 시드, 로그인, Rule Engine 배정, Excel·PDF·ZIP
  내보내기를 실제로 수행하고, 하나라도 실패하면 릴리즈가 중단됩니다.
- 한글 폰트가 빠진 image는 PDF 내보내기만 실패합니다. 그 폰트는 image 안에만 있으므로
  코드 테스트가 전부 통과해도 잡히지 않던 결함이며, 이제 릴리즈 경로에서 막힙니다.

이번 릴리즈는 서비스 동작 변경이 없습니다.

## v0.43.0

### 기능
- **`seccheck selftest` 명령이 추가되었습니다.** 운영 가이드의 업그레이드 절차는 새
  image를 검증 환경에서 띄우고 준비 상태, 로그인, Snapshot, 증적 다운로드를 확인하라고
  적어 두었지만, 그 확인은 전부 사람이 눈으로 하는 일이었습니다. 이제 같은 점검을
  실행하고 종료 코드로 답합니다.
- 기본은 읽기 전용(준비 상태, 로그인, OpenAPI, 게시 템플릿, 감사 체인 검증)이며,
  `--full`은 심의를 하나 만들어 Rule Engine 배정과 Excel·PDF·ZIP 내보내기까지
  확인합니다. **심의를 생성하므로 운영 환경이 아닌 검증 환경에서 사용하십시오.**
- **한글 폰트가 빠진 image는 이 단계에서만 드러납니다.** PDF 내보내기는 폰트가 설치된
  image 안에서만 성공하므로, 테스트 스위트가 전부 통과해도 남을 수 있는 패키징 결함을
  잡습니다.
- `--base-url`로 다른 호스트를 점검할 수 있고, `--json`은 파이프라인용이며, 비밀번호는
  `SECCHECK_SELFTEST_PASSWORD`로도 전달합니다.

이번 릴리즈는 서비스 동작 변경이 없습니다.

## v0.42.0

### 기능
- **검토자가 항목을 일괄 판정할 수 있습니다.** 작성자는 처음부터 `일괄 작성`을 쓸 수
  있었는데, 같은 항목들을 하나씩 다시 훑는 검토자에게는 그런 수단이 없었습니다. 반복
  되는 판정이 검토를 반나절짜리 일로 만드는 주된 원인입니다. 목록에서 항목을 선택하고
  `일괄 판정`으로 검토 결과·최종 적용 여부·증적 충분성·공통 의견을 한 번에 적용합니다.
- 이미 판정한 항목은 기본적으로 건너뜁니다. `덮어쓰기`를 명시해야 교체되므로, 개별로
  내린 판정이 일괄 작업에 조용히 지워지지 않습니다.
- 검토 중(`REVIEWING`) 상태에서 배정된 검토자만 가능하며, 한 번에 1000개까지 처리하고
  감사로그에 `검토 결과 일괄 판정`으로 기록됩니다.

## v0.41.0

### 기능
- **사용자 목록에 `마지막 접속`이 표시됩니다.** 값은 이미 API가 내려주고 있었는데
  화면에 없었습니다. 계정별 최근 로그인 일시와 경과 일수를 보여 주며, 한 번도
  로그인하지 않은 계정은 생성일과 함께 `기록 없음`으로 표시합니다.
- **접근 검토용 필터를 추가했습니다.** `장기 미접속 관리자 잠금` 기준일에 근접한 권한
  계정은 경고로, 기준을 넘긴 계정은 `잠금 대상`으로 표시하고, `N일 이상 미접속`
  필터로 회수 대상을 한 번에 조회합니다. 기준일은 실제 설정값을 읽으므로 90일에서
  바꿔 둔 설치본에서도 맞습니다.
- 이 제품은 계정을 자동으로 잠그면서도 누가 잠금에 가까워지는지 볼 방법이 없었고,
  정기 접근 검토는 이 제품이 다루는 주제이기도 합니다.

## v0.40.0

### 기능
- **체크리스트 항목을 순차로 이동할 수 있습니다.** 검토자는 한 심의에서 수백 개
  항목을 차례로 처리하는데, 지금까지는 목록에서 매번 찾아 클릭하는 방법밖에
  없었습니다. `항목 상세` 우측 상단의 위·아래 버튼과 `Alt`+`↑`/`↓` 단축키가
  추가되었고, 현재 위치가 `34 / 134`처럼 표시됩니다.
- 이동 순서는 화면에 적용된 검색어와 필터를 그대로 따릅니다. 예를 들어 `미작성`
  필터를 켜면 미작성 항목만 차례로 넘어갑니다.
- 단축키에 `Alt`를 요구하므로 답변을 입력하는 도중에도 오작동하지 않고, 입력 중에도
  다음 항목으로 넘어갈 수 있습니다.

## v0.39.0

### 테스트
- **다른 사람이 교체한 증적을 복호화하는 경로에 회귀 테스트를 추가했습니다.** 증적은
  업로더 개인 키로 암호화되므로, 두 번째 사람이 새 버전을 올리면 그 파일은 그 사람의
  키로만 읽힙니다. 코드는 증적 행에 기록된 키 소유자를 사용해 올바르게 동작하지만,
  이를 "내려받는 사람의 키"로 바꾸는 흔한 단순화를 막을 검증이 없었습니다. 실제로
  그렇게 바꿔 테스트가 실패하는 것까지 확인했습니다.

이번 릴리즈는 동작 변경이 없습니다.

## v0.38.0

### 보안
- **`/metrics` 공개 여부를 설정할 수 있습니다.** 인증 없이 응답하도록 되어 있었고
  이는 Prometheus 스크랩의 일반적인 방식이지만, 공개되는 값에는 사용자 수, 누적 심의
  건수, **24시간 로그인 실패 수**, 잠긴 계정 수, 증적 저장 용량이 포함됩니다. 어디에도
  이 사실이 적혀 있지 않아 배치 담당자가 판단할 근거가 없었습니다. 관리자 설정 >
  접근 보안에 `/metrics를 인증 없이 공개`가 추가되었고, 끄면 읽기 범위 API 키로
  수집합니다.

### 설정
- `security.metrics_public` (기본 `true`). **기본값은 기존 동작과 같으므로 업그레이드
  시 스크랩이 끊기지 않습니다.** 신뢰 경계 밖에서 접근 가능한 배치라면 끄는 것을
  권장하며, 운영 가이드에 API 키를 쓰는 `scrape_configs` 예시를 실었습니다.

## v0.37.0

### 보안
- **심의 접근 통제를 HTTP 수준에서 전수 검증합니다.** 참여 여부로 판단하는 20개
  엔드포인트 전부에 대해, 해당 심의와 아무 관계가 없는 사용자가 호출하면 어떤 것도
  응답하지 않는지 실제로 확인합니다. 이 사용자에게는 `SYSTEM_ADMIN`을 포함해 조회
  권한과 무관한 모든 역할을 부여했으므로, 막는 것은 오직 접근 권한 확인입니다.
  참여 확인 없이 새 엔드포인트가 추가되면 CI가 실패합니다.

### 문서
- **직전 릴리즈의 `x-object-scoped` 설명을 바로잡았습니다.** "참여자만 호출 가능"이라고
  적었는데, 조회는 `SECURITY_REVIEWER`와 `AUDITOR`도 모든 심의에 접근할 수 있습니다
  (감사자가 전수 열람을 못 하면 감사가 성립하지 않습니다). 반대로 `SYSTEM_ADMIN`은
  포함되지 않습니다 — 서비스 운영 권한이 남의 심의 열람 권한은 아니며, 이 구분은
  코드에만 있고 어디에도 적혀 있지 않았습니다.

## v0.36.0

### 기능
- **역할이 아니라 심의 참여 여부로 판단하는 API를 명세에 표시합니다.** 20개
  엔드포인트는 역할 제한이 없는 대신 대상 심의의 참여자인지를 확인하는데,
  `x-required-roles`가 비어 있다는 사실만으로는 "누구나 호출 가능"으로 읽혔습니다.
  실제와 정반대입니다. 이제 `x-object-scoped: true`와 설명이 함께 나가며, 권한이
  없으면 404를 반환한다는 점도 명시합니다.

### 문서
- **API 가이드의 필요 역할 표기 7건을 바로잡았습니다.** 서버가 요구하지 않는 역할을
  적어 둔 곳(`POST /review-requests`의 `SYSTEM_ADMIN`, Control 영향 조회의
  `TEMPLATE_ADMIN`)과, 실제로는 심의 참여 여부로 판단하는데 역할로 적어 둔 곳이
  섞여 있었습니다.
- 가이드의 역할 표기가 서버와 어긋나거나, 참여자 기준 판단 목록이 핸들러와 달라지면
  CI가 실패합니다. 두 목록 모두 핸들러에서 AST로 다시 도출해 대조합니다.

## v0.35.0

### 기능
- **OpenAPI 명세가 요청 본문을 설명합니다.** 지금까지 명세에는 `requestBody`가 하나도
  없었습니다. 모든 엔드포인트가 문서화되어 있다고 적어 두었지만, 정작 무엇을 보내야
  하는지는 어디에도 없어서 연계 담당자가 소스를 읽거나 추측해야 했습니다. 이제 본문을
  받는 35개 operation에 속성 이름과 타입이 들어갑니다. 서버가 알 수 없는 속성을
  거부하므로 스키마도 `additionalProperties: false`이며, 명세대로 보내면 그대로
  통과합니다.
- 이 표는 핸들러가 실제로 디코드하는 구조체에서 AST로 다시 도출해 대조하므로, 코드와
  명세가 어긋나면 CI가 실패합니다.

### 수정
- **최종 승인·반려를 본문 없이 호출할 수 있습니다.** 의견은 선택 항목인데도 본문이
  없으면 `400 INVALID_JSON`으로 거부했습니다. 명세에 본문 설명이 없던 상태에서
  연계 클라이언트가 할 만한 가장 자연스러운 호출이 바로 이것이었습니다.

## v0.34.0

### 보안
- **디렉터리가 바꾼 역할도 감사로그에 남깁니다.** 지난 릴리즈에서 추가한 그룹 매핑은
  사람의 결정 없이 로그인마다 권한을 바꾸는데, 그 변화가 어디에도 기록되지 않았습니다.
  실제로 역할이 달라진 경우에만 `디렉터리 역할 동기화` 이벤트가 변경 전후 역할과 함께
  기록되며, 변화 없는 로그인은 기록하지 않습니다.

### 기능
- **토큰에서 읽은 그룹 목록을 서버 로그에 남깁니다.** Keycloak은 Client에 group
  membership mapper를 추가해야 토큰에 그룹을 넣어 주는데, 그게 빠져 있으면 매핑이
  잘못된 것과 증상이 같습니다. 이제 `oidc` 구성요소 로그에서 실제로 무엇이 도착했는지
  확인해 두 경우를 구분할 수 있습니다.

### 문서
- README의 템플릿 상세 설명에서 존재하지 않는 `가중치`를 실제 기능인 `중요도`로
  바로잡았습니다.

## v0.33.0

### 기능
- **OIDC 그룹 → 역할 매핑을 구현했습니다.** 기능 가이드는 그룹 매핑을 지원한다고
  적어 두었지만 실제로는 존재하지 않았고, OIDC 사용자는 최초 로그인 때 기본 역할
  하나를 받은 뒤 관리자가 손으로 바꿔 주기 전까지 그대로였습니다. 관리자 설정 >
  Keycloak OIDC에 `그룹 Claim`과 `그룹 → 역할 매핑`이 추가되었습니다.
- **매핑을 쓰면 권한 회수가 자동입니다.** 매핑을 하나라도 지정하면 로그인할 때마다
  그룹을 기준으로 역할을 다시 맞추므로, IdP 그룹에서 빠진 사용자는 다음 로그인에
  해당 역할을 잃습니다. 그룹 이름은 앞의 `/` 유무와 대소문자를 가리지 않습니다.

### 설정
- `oidc.groups_claim` (기본 `groups`), `oidc.role_mappings` (`{group, role}` 목록)이
  추가되었습니다. **매핑을 지정하지 않으면 기존 동작 그대로입니다.**
- **시스템 관리자는 그룹으로 부여할 수 없습니다.** 역할 동기화는 사람의 확인 없이
  로그인마다 일어나므로, 디렉터리 그룹 편집 권한이 곧 감사 시스템 관리자 권한이 되는
  경로를 만들지 않기 위한 결정입니다. 매핑 대상이 아닌 역할은 동기화가 건드리지 않으므로
  관리자 화면에서 직접 부여한 값은 유지됩니다.

## v0.32.0

### 수정
- **최종 승인 대기 상태에서 영구히 멈추던 심의를 풀었습니다.** 제출 시점에는 승인
  프로세스가 꺼져 있었다가 나중에 켜지면, 심의는 승인자가 지정되지 않은 채
  `APPROVAL_PENDING`으로 넘어갑니다. 이 건은 모든 승인자의 대기 목록에 보이지만
  결재는 지정된 승인자만 할 수 있어 아무도 처리할 수 없었고, 이후 승인자를 지정할
  방법도 없었습니다. 이제 승인자 권한을 가진 사람이 결재할 수 있고, 실제로 결재한
  사람이 승인자로 기록됩니다.
- **지정된 승인자가 퇴사하면 시스템 관리자가 승인자를 변경할 수 있습니다.** 승인 대기
  상태에서는 어떤 항목도 수정할 수 없었기 때문에 그때까지는 되돌릴 방법이 없었습니다.
  변경 대상은 승인자 권한을 가진 사용자여야 하며, `최종 승인자 변경` 감사 이벤트가
  남고 새 승인자에게 알림이 발송됩니다. 관리자가 이 상태에서 바꿀 수 있는 항목은
  승인자뿐입니다.

## v0.31.0

### 보안
- **읽기 전용 API 키의 쓰기 범위를 MCP 도구 단위로 확인합니다.** JSON-RPC는 읽기도
  POST로 오기 때문에 `/mcp`는 미들웨어의 쓰기 범위 검사에서 제외되어 있습니다. 이 예외는
  모든 MCP 도구가 읽기 전용일 때에만 성립하는데, 그 전제를 강제하는 장치가 없어 나중에
  쓰기 도구가 추가되면 읽기 전용 키로 호출할 수 있었습니다. 이제 호출되는 도구가
  읽기 전용으로 선언되어 있는지 확인하며, 카탈로그에 없는 이름은 읽기 전용으로 보지
  않습니다. 현재 도구 7개는 모두 읽기 전용이므로 동작 변화는 없습니다.
- 모든 MCP 도구가 읽기 전용으로 선언되어 있는지, 그리고 응답하는 도구가 전부
  `tools/list`에 있는지 CI가 확인합니다.

## v0.30.0

### 수정
- **Excel 가져오기가 같은 파일에서 매번 다른 결과를 낼 수 있던 문제를 고쳤습니다.**
  컬럼 자동 매핑을 Go map 순회 순서로 결정하고 있어, 같은 워크북을 두 번 올리면
  서로 다른 매핑이 제시됐습니다. 마법사는 제시된 매핑을 그대로 제출하므로 결과 템플릿도
  달라졌습니다. 특히 `구분 No.` 컬럼이 `구분`(section)으로 잡히는 경우가 잦았고, 그러면
  **항목코드 컬럼이 없는 것으로 처리되어 원본 항목코드 대신 자동 코드가 부여**됐습니다.
  이제 정확히 일치하는 별칭이 부분 일치를 이기고, 한 컬럼은 하나의 필드만 차지하며,
  같은 파일은 항상 같은 매핑을 냅니다.
- `점검항목`은 보안요건 제목이 아니라 점검 질문으로만 인식합니다. 두 필드의 별칭에 함께
  들어 있어 구분이 불가능했습니다. 점검항목만 있는 Sheet는 기존대로 질문에서 제목을
  만들어 냅니다.

**주의**: 자동 매핑 기본값이 달라집니다. 가져오기 전 마법사의 컬럼 매핑을 확인하십시오.
이전에 가져온 템플릿은 영향을 받지 않습니다.

## v0.29.0

### 기능
- **`/metrics`에 큐 정체 지표를 추가했습니다.** `seccheck_jobs_oldest_pending_seconds`는
  실행 시각이 지난 가장 오래된 작업의 대기 시간입니다. 기존 `seccheck_jobs_pending`은
  재시도 대기 작업까지 세기 때문에 단독 경보 기준으로 쓸 수 없었고, 정체를 감지할
  지표가 아예 없었습니다. `> 900`이면 워커 정지를 의심할 수 있습니다.

### 문서
- **운영 가이드에 `/metrics` 전체 지표 표를 실었습니다.** 지금까지는 산문 한 줄로만
  요약되어 있어 경보 규칙을 쓰려면 소스를 읽어야 했습니다. 인터넷이 닿지 않는 환경에서는
  매뉴얼이 유일한 참조입니다. 지표를 추가하고 표에 넣지 않으면 CI가 실패합니다.

## v0.28.0

### 수정
- **CSV 내보내기가 조용히 잘리던 문제를 고쳤습니다.** 감사로그와 심의 목록 CSV는 최대
  50,000행에서 멈추는데, 아무 표시가 없어 완전한 내보내기처럼 보였습니다. 감사 대응처럼
  파일 자체가 근거가 되는 상황에서 가장 위험한 실패 방식입니다. 이제 행 수가 상한에
  닿으면 `가장 최근 50,000건까지만 내보냈습니다`라고 알리고, 감사로그에도
  `truncated` 값이 남습니다. 상한 자체는 한 번의 내려받기가 설치본 전체 이력을
  메모리로 끌어오지 못하게 하는 안전장치이므로 유지합니다.
- 증적 삭제 버튼에 파일명을 포함한 접근성 레이블을 붙였습니다. 화면 낭독기에서 어떤
  증적을 지우는 버튼인지 구분되지 않았습니다.

## v0.27.0

### 수정
- **내려받기가 실패하면 브라우저가 JSON 오류 문서로 이동하던 문제를 고쳤습니다.** 심의
  Excel·PDF·ZIP 내보내기, 증적 원본 내려받기, 감사로그·심의 목록 CSV, 심의 리포트
  Excel은 모두 API 주소를 가리키는 링크였습니다. 요청이 실패하면 브라우저가 그 주소로
  이동해 보던 화면을 잃고 기계용 JSON을 보게 됐습니다. 한글 폰트가 없는 환경의 PDF
  내보내기, 검사가 끝나지 않은 증적 내려받기(409)가 실제로 이 경로였습니다. 이제
  실패 사유가 화면 위 토스트로 표시되고 보던 화면은 그대로 남습니다.
- 파일 이름은 서버가 지정한 이름을 그대로 사용합니다(한글 파일명 포함).
- 앞으로 화면에서 API 주소로 직접 링크하면 CI가 실패합니다.

## v0.26.0

### 수정
- **첫 로딩이 실패하면 화면이 영원히 스피너에 머물던 문제를 고쳤습니다.** 대시보드,
  서비스 설정, 템플릿 상세, 작업 큐, 알림, 계정 보안, 심의 상세 7개 화면은 데이터가
  올 때까지 본문 전체를 스피너로 가립니다. 서버 오류나 네트워크 단절로 첫 요청이
  실패하면 아무 설명 없이 스피너가 계속 돌았고, 일부 화면은 몇 초 뒤 사라지는 토스트만
  띄운 뒤 역시 스피너에 머물렀습니다. 이제 실패 사유와 `다시 시도` 버튼이 화면에
  남습니다. 세션 만료(401)는 기존대로 로그인 화면으로 안내합니다.
- 앞으로 스피너로 본문을 가리는 화면에 실패 처리가 없으면 CI가 실패합니다.

## v0.25.0

### 기능
- **큐가 멈추면 관리자에게 인앱 알림이 갑니다.** 큐가 멈췄다는 사실은 이메일로 알릴 수
  없습니다 — 이메일을 보내는 워커가 바로 그 멈춘 워커이기 때문입니다. 유지보수 워커가
  매시간 큐를 확인해, 실행 시각이 지난 작업이 15분 넘게 대기 중이면 시스템 관리자에게
  알림을 남깁니다. 같은 장애로 6시간 안에 이미 알린 관리자는 건너뛰므로 알림함이
  묻히지 않습니다.
- 관리자 알림에서 해당 화면으로 바로 이동합니다. 작업 큐 정체는 작업 큐로, 감사로그
  무결성 실패는 감사로그로 연결됩니다. 지금까지는 알림만 보고 화면은 직접 찾아가야
  했습니다.

### 설정
- 알림 수신 설정에 `작업 큐 정체` 항목이 추가되었습니다 (시스템 관리자).

## v0.24.0

### 기능
- **큐가 멈추면 작업 큐 화면이 알려줍니다.** 워커가 죽으면 이메일 알림과 증적 검사가 조용히
  멈추는데, 화면에는 평범해 보이는 대기(PENDING) 작업만 남아 관리자가 알아챌 방법이
  없었습니다. 이제 실행 시각이 지난 가장 오래된 작업의 대기 시간을 집계해, 5분을 넘기면
  경고와 함께 확인할 로그 구성요소를 안내합니다. 재시도 대기(backoff) 중인 작업은
  아직 실행 시각이 오지 않았으므로 정체로 세지 않습니다.

### 문서
- **기능 가이드에 빠져 있던 4개 메뉴를 추가했습니다.** 심의 리포트, Rule Engine 시뮬레이터,
  작업 큐, 계정 보안은 화면에는 있었지만 `docs/features.md`에 설명이 없었습니다. 앞으로
  메뉴를 추가하면 가이드에 항목이 없을 때 CI가 실패합니다.

## v0.23.0

### 기능
- **신규 심의 요청 화면에서 배정될 체크리스트를 미리 봅니다.** 서비스 특성 9개 토글이 전체
  체크리스트를 결정하는데, 그 효과는 심의를 만든 뒤에야 알 수 있었습니다. 요청자는 무엇이
  배정될지 모르는 채로 답해야 했습니다. 이제 특성을 바꿀 때마다 배정될 항목 수와 템플릿별
  내역이 즉시 갱신됩니다.
- Rule Engine 시뮬레이션을 심의 요청자도 사용할 수 있습니다. 게시된 템플릿의 메타데이터만
  읽으며, 이는 심의를 생성하는 즉시 어차피 보게 되는 정보입니다. 심의를 만들지 않습니다.

## v0.22.0

### 수정
- **API·MCP 연계 화면의 도구 목록이 오래된 상태였습니다.** 화면에 5개가 적혀 있었지만 실제로는
  7개를 제공하고 있어(`seccheck.my_queue`, `seccheck.review_report`), 이 화면을 보고 연계를
  구성하는 사람은 두 도구의 존재를 알 수 없었습니다. OpenAPI 명세와 같은 종류의 드리프트입니다.
  이제 서버가 실제 도구 정의에서 목록을 제공하므로 다시 어긋날 수 없습니다.
- 연계 화면이 MCP 프로토콜 버전과 호환 버전, 도구별 설명과 읽기 전용 여부도 서버에서 받아
  표시합니다.

### 테스트
- 화면에 노출되는 도구 목록이 실제 제공 목록과 일치하는지, 프로토콜 버전이 같은지, 모든 도구가
  읽기 전용인지(화면이 그렇게 약속하므로) 검사합니다.

## v0.21.0

### 릴리즈 파이프라인
- **CI가 실패한 커밋도 릴리즈될 수 있었습니다.** v0.20.0이 그렇게 나갔습니다. 릴리즈 작업에는
  PostgreSQL이 없어 통합 테스트가 조용히 skip되었고, govulncheck·gitleaks·DAST 게이트도 릴리즈
  경로에는 아예 없었습니다.
  - 릴리즈 워크플로가 해당 커밋의 `security-ci` 결과를 확인하고, 실패했거나 CI 기록이 없으면
    중단합니다. CI가 진행 중이면 최대 20분 기다립니다.
  - 릴리즈 작업에도 PostgreSQL service를 붙여 통합 테스트가 실제로 실행됩니다.

## v0.20.1

### 수정
- v0.20.0에 추가한 내보내기 테스트가 한글 글꼴이 없는 환경에서 실패했습니다. PDF 생성은 호스트의
  한글 글꼴에 의존하며 릴리즈 이미지에는 `fonts-nanum`이 포함되지만, 글꼴이 없는 러너에서는
  `FONT_MISSING`이 정상 동작입니다. 테스트가 두 경우를 모두 인정하도록 고쳤습니다.
  제품 동작은 변경되지 않았습니다.
- **관리자 > 시스템 정보에 PDF 글꼴 설치 여부를 표시합니다.** 이미지를 변경해 운영하는 곳에서
  글꼴이 빠지면 지금까지는 사용자가 PDF 내보내기에 실패해야 알 수 있었습니다.

## v0.20.0

### 기능
- **내보낸 보고서에 첨부 증적이 기록됩니다.** Excel과 PDF는 답변과 검토 결과는 담았지만
  **무엇을 근거로 첨부했는지는 담지 않았습니다.** 보고서를 받아 보는 사람은 증적이 실제로
  있었는지 주장뿐이었는지 구분할 수 없었습니다.
  - Excel 항목 시트에 `첨부 증적` 컬럼이 추가되고, `증적 목록` 시트에 항목코드·보안요건·파일명·
    버전·SHA-256이 정리됩니다. ZIP을 열지 않고도 보고서와 파일을 대조할 수 있습니다.
  - PDF 항목별 결과에 파일명·버전·SHA-256이 함께 출력됩니다.

## v0.19.0

### 기능
- **심의가 어느 체크리스트 버전 기준인지 표시합니다.** 제출 시점 스냅샷은 이후 템플릿이 바뀌어도
  움직이지 않는 것이 설계 의도인데, 이를 알려주지 않아 오늘의 체크리스트와 비교하는 검토자가
  자신이 이전 판본을 보고 있다는 사실을 알 수 없었습니다. 심의 상세에 `최신 체크리스트 기준`
  또는 `이전 체크리스트 기준 N건`을 표시하고, 어떤 템플릿이 몇 버전에서 몇 버전으로 바뀌었는지
  함께 보여줍니다.

### 테스트
- 제품의 핵심 약속인 **"게시 후 불변인 체크리스트 버전과 제출 시점 Snapshot"** 에 회귀 테스트가
  없었습니다. 이제 게시된 버전에 대한 항목 추가·수정·삭제가 모두 409로 거부되는지, 항목 없는
  버전 게시와 중복 게시와 초안 사용 중지가 거부되는지, 그리고 진행 중인 심의의 체크리스트가
  새 버전 게시 후에도 그대로인지를 검증합니다. 세 경로 모두 정상 동작을 확인했습니다.

## v0.18.0

### 기능
- **심의별 이력**: 심의 상세에 `이력` 버튼이 생겼습니다. 생성·작성·제출·보완 요청·검토·승인 등
  해당 심의에서 일어난 일을 시각과 수행자와 함께 보여줍니다. 감사로그에는 처음부터 모두
  기록되어 있었지만 **시스템 관리자와 감사자만 볼 수 있어**, 정작 일하는 사람은 "보완 요청이
  언제 왔지", "왜 반려됐지"를 자기 심의에서 확인할 수 없었습니다. 심의를 볼 수 있는 사람만
  그 심의의 이력을 볼 수 있으며, 다른 심의의 기록은 섞이지 않습니다.
- 항목 단위 기록에는 해당 체크리스트 항목코드가 함께 표시됩니다.

### 수정
- 일회용 코드 등록이 필요한 상태에서 알림 배지가 1분마다 차단되는 요청을 계속 보내던 문제를
  고쳤습니다. 등록 화면에 머무는 동안에는 폴링하지 않습니다.

### 스키마
- **008**: 감사로그 대상 조회용 인덱스를 추가합니다.

## v0.17.0

### 수정 (중요)
- **유휴 세션 만료가 브라우저 탭이 열려 있으면 절대 동작하지 않았습니다.** v0.3.0에서 추가한
  읽지 않은 알림 배지가 1분마다 서버를 호출하는데, 이 호출이 세션 활동으로 계산되어
  `last_seen_at`을 계속 갱신했습니다. 자리를 비운 채 열어둔 탭을 닫으려던 설정을 **자체 폴링이
  무력화**하고 있었습니다. 이제 배지 조회는 활동으로 계산하지 않습니다. 서버가 판단하므로
  클라이언트가 헤더를 빼서 세션을 연장할 수 없습니다.
- **세션이 끝나도 화면이 로그인으로 돌아가지 않았습니다.** 유휴 만료, 비밀번호 변경, 관리자의
  세션 종료로 세션이 사라지면 이후 모든 호출이 401이 되는데, 각 화면이 오류 토스트만 띄우고
  동작하지 않는 페이지에 사용자를 남겨뒀습니다. 이제 로그인 화면으로 돌아가며 이유를 안내합니다.
- **관리 설정 변경이 최대 15초 뒤에 적용됐습니다.** 설정을 바꾸고 바로 확인한 관리자는 예전
  동작을 보고 설정이 안 먹는다고 판단할 수밖에 없었습니다. 저장 즉시 캐시를 무효화합니다.

## v0.16.0

### 수정
- **OpenAPI 명세가 API의 3분의 1만 설명하고 있었습니다.** 등록된 91개 경로 중 31개만 손으로 적힌
  명세에 있었고, 나머지 60개는 문서에 존재하지 않았습니다. README가 "REST/OpenAPI 연계"를 내세우는
  제품에서 명세를 믿은 연계 담당자에게는 없는 것보다 나쁜 상태였습니다.
  이제 **라우트 등록 테이블에서 명세를 생성**하므로 문서화되지 않은 엔드포인트가 존재할 수 없습니다.

### 기능
- 명세에 경로 파라미터 선언, 안정적인 `operationId`, 태그 목록, 그리고 엔드포인트별 필요 역할
  (`x-required-roles`)이 포함됩니다. 인증 없이 호출 가능한 엔드포인트는 `security: []`로 명시합니다.
- `/api/openapi.json`의 `info.description`에 역할 표기 방식을 설명했습니다.

### 테스트
- 등록된 모든 API 라우트에 요약과 태그가 있는지, `operationId`가 유일한지 검사합니다. 생성된
  문서를 HTTP로 받아 경로 수, 파라미터 선언, 역할 표기, 공개 엔드포인트 표시까지 확인합니다.

## v0.15.0

### 주의 (API 변경)
- `GET /api/v1/admin/audit`가 배열 대신 `{items, events}`를 반환합니다. `items`는 기존 목록이고
  `events`는 필터용 이벤트 유형 카탈로그입니다. 각 행에는 한글 이름 `event_label`이 추가됩니다.

### 수정
- **심의 제출 알림이 내부 UUID를 표시했습니다.** "심의 7a988578-…이(가) 제출되었습니다"처럼
  나가던 것을 심의번호와 서비스명으로 바꿨습니다.
- **제출 알림의 이벤트 유형이 수신 설정 목록에 없었습니다.** `SUBMISSION`으로 발송되는데 목록에는
  `REVIEW_SUBMITTED`가 있어, 사용자가 이 알림만 끌 수 없었고 화면에도 코드가 그대로 노출됐습니다.
- 승인·반려 알림 제목이 `심의 APPROVED` / `심의 REJECTED`처럼 영문 코드였습니다.

### 기능
- **감사로그 이벤트 한글 이름**: 59개 이벤트 유형에 이름을 붙였습니다. 목록과 상세, CSV 첫 컬럼에
  표시되며, 필터는 자유 입력 대신 카탈로그에서 고를 수 있습니다. 지금까지 감사자는
  `UPDATE_SUBMISSION` 같은 코드를 그대로 읽어야 했습니다.

### 테스트
- 기록되는 모든 감사 이벤트에 이름이 있는지, 발송되는 모든 알림이 수신 설정 목록에 있는지
  검사하는 테스트를 추가했습니다. 목록에만 있고 실제로는 발송되지 않는 항목도 함께 잡습니다.

## v0.14.0

### 수정 (중요)
- **심의 참여자로 추가하면 누구나 체크리스트를 수정할 수 있었습니다.** `review_participants.participant_role`
  컬럼은 최초 스키마부터 `CONTRIBUTOR` 기본값으로 존재했지만 읽지도 쓰지도 않아, 참고 목적으로
  추가한 사람에게도 쓰기 권한이 부여됐습니다. 이제 `CONTRIBUTOR`(작성 가능)와 `VIEWER`(열람 전용)를
  구분하며, VIEWER에게는 항목 배정도 거부합니다. 기존 참여자는 모두 `CONTRIBUTOR`로 유지되므로
  동작이 바뀌지 않습니다.
- 악성코드 검사 결과 상세(`evidences.scan_detail`)를 실제로 저장하고 화면에 표시합니다.
  v0.4.0에서 컬럼만 추가하고 기록하지 않아, 차단 사유를 로그에서만 확인할 수 있었습니다.

### 스키마
- **007**: `review_participants.participant_role`에 값 검증 제약을 추가하고,
  한 번도 사용되지 않은 `notifications.channel` 컬럼을 제거합니다. 이 컬럼은 어떤 버전도
  INSERT에서 명시한 적이 없어 롤백에 영향을 주지 않습니다.

### 테스트
- 스키마 컬럼과 기본 설정 키 전체를 Go 소스와 대조해, 한 번도 참조되지 않는 항목이 있으면 빌드가
  실패하는 테스트를 추가했습니다. 지금까지 같은 유형의 문제가 네 번 있었습니다 — 보존 기간,
  표시 시간대, 항목 담당자, 증적 파일 보존. 모두 화면에는 있는데 아무 일도 하지 않았습니다.

## v0.13.0

### 수정 (중요)
- **삭제한 증적의 암호문이 볼륨에 영구히 남아 있었습니다.** 증적 삭제는 `deleted_at`만 기록했고,
  코드 주석은 "물리 파일은 설정된 보존 정책에 따라 유지된다"고 했지만 **그런 정책은 구현되어 있지
  않았습니다.** 오프라인 어플라이언스의 증적 볼륨이 줄어들 방법이 없었습니다.
  이제 유지보수 작업이 `upload.deleted_evidence_retention_days`(기본 90일)가 지난 삭제 증적의
  암호문을 제거합니다. **메타데이터 행(파일명·SHA-256·삭제자·삭제 시각)은 보존**하므로 감사
  기록은 그대로 남습니다.

### 스키마
- **006**: `evidences.purged_at`, `evidence_versions.purged_at`, 업로드 설정
  `deleted_evidence_retention_days`(기본 90)를 추가합니다.

### 기능
- `verify-evidence`가 **고아 파일**(데이터베이스에 대응 레코드가 없는 암호문)을 함께 보고합니다.
  자동 삭제하지는 않습니다. 볼륨의 예상치 못한 파일은 운영자가 확인할 사안이지 유지보수 작업이
  조용히 지울 일이 아니기 때문입니다. 이미 정리된(purged) 증적은 검증 대상에서 제외합니다.

## v0.12.0

### 수정
- **심의번호 연도가 설정된 시간대를 따릅니다.** 컨테이너는 UTC로 도는데 연도와 연 경계를 UTC
  기준으로 계산해, `Asia/Seoul` 설치에서는 새해 첫 9시간에 생성된 심의에 **전년도 번호**가
  붙었습니다. 문서 번호가 가장 중요한 시점에 어긋나던 문제입니다.
- **재심의 복사 시 답변 레코드 ID가 8자리 hex였습니다.** `responses.id`는 기본키인데 충돌 확률이
  누적되는 구조였습니다. UUID로 변경했습니다.

### 기능
- **재심의 복사 결과 안내**: 재심의는 오늘 게시된 템플릿으로 새로 구성되므로 이전 심의와 항목이
  다를 수 있는데, 지금까지는 아무 설명이 없어 직접 스크롤해서 확인해야 했습니다. 이제 복사된
  답변 수, 신설되어 새로 작성해야 하는 항목 수, 현재 템플릿에 없어 제외된 항목 수를 함께
  안내합니다.

## v0.11.0

### 수정
- **항목 코멘트가 아무에게도 전달되지 않던 문제를 고쳤습니다.** 보안 담당자가 질문을 남겨도
  작성자는 우연히 그 항목을 열어보기 전까지 알 수 없었습니다. 이제 대화의 반대편에게 알림이
  갑니다. 검토자가 쓰면 요청자·구축자·개발자에게, 작성자 측이 쓰면 배정된 검토자에게 전달되며,
  항목 담당자가 지정되어 있으면 함께 받습니다. 작성자 본인은 제외됩니다.

### 기능
- **증적 이미지 미리보기**: 증적 대부분이 스크린샷인데 확인하려면 하나씩 내려받아야 했습니다.
  이미지 증적에 `미리보기` 버튼이 생겨 화면에서 바로 확인합니다. 파일은 세션 인증으로 받아
  object URL로 표시하므로 암호화 저장과 접근 통제는 그대로 유지되며, 검사가 끝나지 않은 증적은
  미리보기도 차단됩니다.
- 알림 유형에 `체크리스트 코멘트`가 추가되어 수신 설정에서 개별로 끌 수 있습니다.

## v0.10.0

### 주의 (API 변경)
- `GET /api/v1/templates`와 `GET /api/v1/security-controls`가 배열 대신 심의 목록과 동일한
  `{items, total, limit, offset, has_more}` 봉투를 반환합니다. 이 두 엔드포인트를 직접 호출하는
  연계 스크립트가 있다면 `items`를 읽도록 수정해야 합니다. 기본 페이지 크기는 50이며 최대 200입니다.

### 기능
- **템플릿 목록 페이지네이션과 검색**: 이름·설명·분류 검색, 분류 필터, 사용 여부 필터. 지금까지는
  전체를 한 번에 반환해, 팀별로 워크북을 가져오는 설치에서 목록이 계속 길어졌습니다.
- **Security Control 목록 페이지네이션**: 기존 500건 상한을 페이지네이션으로 대체했습니다.
- **템플릿 편집 화면 항목 검색**: 게시 워크북은 200행이 넘는데 한 장의 표로만 볼 수 있었습니다.
  항목코드·섹션·보안요건·질문·가이드 검색과 중요도 필터를 추가했습니다.

## v0.9.0

### 수정 (중요)
- **동시 편집이 조용히 덮어써지던 문제를 고쳤습니다.** 체크리스트는 공동 작성자와 함께 채우고
  편집기는 1.2초마다 자동 저장하는데, 저장에 버전 검사가 없어 **나중에 저장한 쪽이 상대의 작업을
  말없이 지웠습니다.** 이제 편집기가 불러온 버전을 함께 보내고, 그 사이 다른 사람이 저장했다면
  409로 거부하면서 현재 저장된 내용과 저장한 사람을 함께 반환합니다. 화면에서는 두 버전을 보여주고
  `최신 내용 불러오기` 또는 `내 내용으로 덮어쓰기`를 선택하게 합니다. 보안 담당자의 검토 결과에도
  같은 보호를 적용했습니다.

### 기능
- **항목별 담당자 지정**: `responses.assigned_to` 컬럼은 처음부터 있었지만 화면에 입력 수단이 없어
  쓰이지 않았습니다. 항목 편집기에서 담당자를 지정하고, 목록에 담당자 배지가 표시되며,
  `내 담당 항목` 필터로 자기 몫만 볼 수 있습니다.
- **담당자 일괄 배정**: 항목을 다중 선택해 한 번에 배정합니다. 답변은 건드리지 않으며, 배정된
  사람에게 알림이 배치당 한 번 발송됩니다. 심의에 참여하지 않는 사용자에게는 배정할 수 없습니다.
- 일괄 작성에서도 담당자를 함께 지정할 수 있습니다.
- 알림 유형에 `체크리스트 항목 배정`이 추가되어 수신 설정에서 개별로 끌 수 있습니다.

## v0.8.0

### 기능
- **심의 리포트** (`/reports`): 기간과 부서로 범위를 좁혀 신규·제출·완료·반려·진행 중 건수,
  제출부터 승인까지의 처리 기간(평균·중앙값·90분위), 상태별·부서별 집계, 검토 판정 분포,
  반복 미흡·부적합 항목 Top 20, 진행 중 심의의 경과 기간 분포를 집계합니다.
  지금까지 대시보드에는 전체 기간 누적 수치만 있었고 내보내기가 없어, 월간 보고 자료는 손으로
  만들어야 했습니다.
- **Excel 리포트 내보내기**: 같은 집계를 요약·상태별·부서별·검토 결과·반복 미흡 항목·경과 기간
  6개 시트로 내려받습니다. 대상 기간과 생성 시각(시간대 포함)이 함께 기록됩니다.
- MCP에 `seccheck.review_report` 도구를 추가했습니다. 화면과 동일한 집계 코드를 사용합니다.
- 리포트는 시스템 관리자, 보안 담당자, 감사자, 승인자가 조회할 수 있습니다.

## v0.7.0

### 수정
- **`general.timezone` 설정이 실제로 동작합니다.** 지금까지 이 값은 어디에서도 사용되지 않았고,
  컨테이너가 UTC로 도는 탓에 화면 시각은 브라우저 시간대, 서버가 만드는 것은 UTC였습니다.
  이제 화면·Excel/PDF 내보내기·요약 메일이 모두 설정된 시간대를 따릅니다.
  **`Asia/Seoul`로 설정한 곳에서는 일일 요약 발송 시각이 9시간 당겨집니다**(기존에는 UTC 기준으로
  동작해 8시 설정이 실제로는 17시 KST였습니다). 저장 시 IANA 이름을 검증하며, 알 수 없는 이름은
  거부합니다. 시간대 데이터는 바이너리에 내장했습니다.
- Excel·PDF 내보내기에 `내보낸 시각`과 시간대를 함께 표기하고, 날짜를 ISO 원문 대신 읽을 수 있는
  형식으로 출력합니다.

### 기능
- **Rule Engine 시뮬레이터** (`/templates/rules`): 심의를 만들지 않고 서비스 특성만 입력해 어떤
  체크리스트가 배정되는지 항목 단위로 확인합니다. 제외된 항목은 분류 때문인지 적용 규칙 때문인지
  이유를 함께 보여줍니다. 게시된 버전은 수정할 수 없으므로 게시 전 규칙 검증에 사용하세요.
  체크리스트 관리자와 보안 담당자가 사용할 수 있습니다.

## v0.6.0

### 수정 (중요)
- 요청 본문 구조체에 json 태그가 빠지면 UI가 보내는 snake_case 키가 거부되는 문제를 막는 테스트를
  추가했습니다. `PATCH /api/v1/me`가 v0.1.0부터 동작하지 않았던 원인이며, 같은 실수를 다시 하면
  이제 빌드가 실패합니다. 전체 핸들러를 감사한 결과 남은 사례는 없었습니다.
- **이메일 알림을 켜면 인앱 알림까지 사라지던 문제를 고쳤습니다.** 발송 작업을 큐에 넣는 SQL이
  `jsonb_build_object('notification_id',$2)`로 파라미터 타입을 추론할 수 없어 항상 실패했고,
  같은 트랜잭션이 롤백되면서 알림 레코드까지 함께 사라졌습니다. 오류는 어디에도 기록되지
  않았습니다. `email_enabled`를 켠 설치는 **알림이 전혀 동작하지 않았습니다.**
  이제 실패 시 서버 로그에 남습니다.

### 스키마
- **005**: `notifications.target_type/target_id/emailed_at`, `notification_preferences` 테이블,
  일반 설정 `base_url`, 알림 설정 `digest_hour`를 추가합니다. 기존 알림 중 본문이 심의번호로
  시작하는 것은 링크가 자동으로 연결됩니다.

### 기능
- **Excel 가져오기 미리보기가 실제 드라이런이 되었습니다.** 원본 5행만 보여주던 것을 실제 파서로
  돌려, 생성될 항목 목록과 함께 건너뛸 행 수, 자동 부여될 항목코드 수, 중복으로 `-DUP2`가 붙을
  코드, 매핑되지 않은 컬럼을 미리 알려줍니다. Sheet 자동 선택도 매핑 개수가 아니라 실제 파싱
  결과 기준으로 바뀌었습니다.
- **템플릿 삭제**: 잘못 가져온 템플릿을 삭제할 수 있습니다. 게시된 버전이 있거나 심의 스냅샷에
  사용된 템플릿은 거부하며(409), 이 경우 `사용 중지`로 전환합니다. 지금까지는 되돌릴 방법이
  없었습니다.
- **알림 클릭 이동**: 심의 관련 알림에 `심의 열기` 링크가 생겼습니다. 지금까지는 알림에서 해당
  심의로 갈 방법이 없었습니다.
- **사용자별 수신 설정**: 유형별 이메일 차단과 `즉시 / 하루 한 번 요약` 선택. 인앱 알림은 설정과
  무관하게 항상 기록됩니다.
- **일일 요약 발송**: `digest_hour`(기본 8시) 이후 사용자당 하루 한 번 미발송 알림을 모아 보냅니다.
- **알림 메일 링크**: 일반 설정의 `서비스 주소`를 채우면 메일 본문에 해당 심의 주소가 들어갑니다.
  제목에는 `[SecCheck]` 접두어가 붙습니다.
- **SMTP 테스트 발송**: 관리자 설정 > 알림에서 저장된 설정으로 테스트 메일을 보냅니다. OIDC
  Discovery 테스트와 같은 역할입니다.
- 알림 목록에 읽지 않음 필터, 유형 필터, 페이지네이션을 추가했습니다.

## v0.5.0

### 스키마
- **004**: `pg_trgm` 확장과 부분 일치 검색용 GIN 인덱스 11개, 감사 체인 검증 지점 컬럼
  (`audit_chain_state.verified_sequence/verified_hash/verified_at`), 작업 큐 목록 인덱스를
  추가합니다.
- **주의**: `pg_trgm` 생성에는 확장 설치 권한이 필요합니다. 최소권한 계정이라면 마이그레이션은
  경고만 남기고 계속 진행하며 검색은 순차 스캔으로 동작합니다. 데이터가 많다면 DBA에게
  `CREATE EXTENSION pg_trgm;` 실행을 요청한 뒤 재기동하면 인덱스가 만들어집니다.
- 확장 생성은 advisory lock으로 직렬화되므로 인스턴스 두 대가 동시에 기동해도 안전합니다.

### 기능
- **작업 큐 관리 화면** (`/admin/jobs`): 이메일 알림과 증적 검사 작업의 상태·시도 횟수·마지막
  오류를 확인하고 개별 또는 실패 전체를 재시도합니다. 지금까지는 `/metrics`의 숫자 두 개가
  전부였습니다.
- **증적 무결성 검증 CLI**: `seccheck verify-evidence [--sample N] [--json]`. 모든 증적을
  복호화해 크기와 SHA-256을 DB와 대조하고 실패 시 종료 코드 1을 반환하므로, 운영 가이드가
  요구하는 복구 훈련을 실제로 검증할 수 있습니다.
- **감사 체인 증분 검증**: 마지막 성공 지점 이후만 확인하므로 이벤트가 누적돼도 점검 시간이
  일정합니다. `전체 재검증` 버튼으로 전체 패스를 실행합니다.
- **검증 실패 알림**: 체인 불일치가 발견되면 서버 로그 ERROR와 함께 모든 시스템 관리자에게
  인앱 알림이 발송됩니다. 이전에는 버튼을 누른 사람만 알 수 있었습니다.
- MCP에 `seccheck.my_queue` 도구를 추가했습니다.

### 성능
- Security Control 목록의 건수 집계를 4테이블 GROUP BY 조인에서 상관 서브쿼리로 바꿔, 화면을
  열 때마다 전체 `submission_items`를 집계하지 않습니다.

### CLI
- `seccheck version`으로 이미지 버전을 확인할 수 있고, 알 수 없는 하위 명령은 사용법을 출력하며
  종료 코드 2를 반환합니다.

## v0.4.0

### 스키마
- 마이그레이션이 번호 기반으로 전환되었습니다. `internal/store/migrations/`의 파일이 순서대로,
  각각 하나의 트랜잭션으로 적용되고 `schema_migrations`에 버전별로 기록됩니다. v0.3.0 이하에서
  올라오는 설치는 baseline(1)을 건너뛰고 002, 003만 적용합니다.
- **002**: 조인 경로 30여 개에 인덱스를 추가했습니다. 데이터가 많은 설치에서는 첫 기동 시
  인덱스 생성에 시간이 걸릴 수 있으므로 점검 시간에 배포하세요.
- **003**: `users.totp_secret/totp_enabled/totp_enrolled_at`, `evidences.scan_detail`,
  `change_requests.reminded_at` 컬럼을 추가했습니다.
- **주의**: v0.3.0으로 롤백하려면 이미지만 되돌리면 됩니다. 추가된 컬럼과 인덱스는 구버전에서
  무시되며 데이터 손실은 없습니다.

### 증적 저장 형식
- 증적은 이제 1MB 청크 단위 AES-256-GCM 스트림(`SCE1` 헤더)으로 저장됩니다. 업로드·다운로드·
  ZIP 내보내기가 파일 전체를 메모리에 올리지 않으며, base64 인코딩을 제거해 디스크 사용량이
  약 33% 줄어듭니다.
- **기존 증적은 변환하지 않습니다.** 이전 형식(base64 단일 암호문)도 그대로 읽히므로 별도
  마이그레이션 작업이 필요 없습니다.

### 악성코드 검사
- **주의**: ClamAV 검사가 업로드 요청에서 분리되어 백그라운드 작업 큐로 이동했습니다. 업로드는
  즉시 완료되고 증적은 `PENDING` 상태로 저장됩니다. 검사가 `CLEAN`이 되기 전에는 다운로드,
  ZIP 내보내기, 심의 제출이 모두 거부되므로 fail-closed 보장은 유지됩니다.
- 악성코드가 탐지되면 증적이 즉시 논리 삭제되고 업로더에게 인앱 알림이 발송됩니다.
- 새 지표: `seccheck_evidence_scan_pending`.

### 설정
- `security.require_totp_for_admins` (기본 `false`): 관리자·검토자·승인자 계정에 일회용 코드를
  요구합니다. 켜면 대상 사용자는 다음 로그인 시 계정 보안 화면에서 등록을 마쳐야 다른 기능을
  사용할 수 있습니다.

### 기능
- 로컬 계정 TOTP 2단계 인증(RFC 6238). 등록은 개인 > 계정 보안, 분실 시 관리자가 초기화합니다.
- 본인 로그인 세션 목록과 개별·일괄 종료.
- 심의 목록 페이지네이션, 총건수, 정렬 6종, 담당자·부서·기간·기한초과·내 차례 필터, CSV 내보내기.
- 대시보드 "내 차례" 큐와 보완 조치 기한 목록. 기한 임박·초과 시 인앱 알림이 자동 발송됩니다.
- 체크리스트 항목 다중 선택 후 일괄 작성. 기본은 미작성 항목만 채우고, 덮어쓰기는 선택입니다.

### 수정
- `PATCH /api/v1/me`가 UI가 보내는 `display_name`, `email`, `department`를 거부하던 문제를
  고쳤습니다. 개인 프로필 저장이 v0.1.0부터 동작하지 않았습니다.

## v0.3.0

### 설정
- **주의**: `security.login_rate_limit_per_minute`의 의미가 바뀌었습니다. 이제 **실패한** 로그인만
  계산하며 기본값이 10에서 30으로 올라갔습니다. v0.2.0에서 올라오는 설치는 기존 값 10이
  유지되므로 30으로 조정하는 것을 권장합니다.

### 기능
- 로컬 계정 본인 비밀번호 변경, 관리자 임시 비밀번호 재설정.
- 미읽음 알림 배지, 알림 일괄 읽음 처리.
- 감사로그 이벤트 접두 매칭·IP·기간 필터, 이벤트 상세, UTF-8 BOM CSV 내보내기.
- 서버 로그 통합 검색과 10초 자동 새로고침.
- 체크리스트 미저장 변경 경고, 제출 검증 결과에서 항목 바로가기, 필터별 잔여 건수.

## v0.2.0

### 스키마
- `users.failed_login_count`, `users.locked_until` 추가.

### 설정
- `security.max_login_failures` (5), `security.lockout_minutes` (15),
  `security.idle_timeout_minutes` (0=미사용), `security.trusted_proxies` (빈 목록) 추가.
- **주의**: Reverse Proxy 뒤에 배치한 설치는 `trusted_proxies` 설정이 필요합니다. 비워 두면 모든
  요청이 Proxy 한 IP로 집계되어 요청 제한과 감사로그의 접속 IP가 조직 단위로 묶입니다.

### 기능
- 로그인 실패 기반 계정 잠금과 관리자 잠금 해제, 유휴 세션 만료.
- 만료 세션·OIDC state·완료 Job·보존 기간 경과 로그의 시간당 자동 정리.
- 새 지표: `seccheck_sessions_active`, `seccheck_accounts_locked`.

### 보안
- Go 1.26.6으로 빌드. stdlib HIGH 7건(crypto/tls, net/http, net/url, encoding/xml,
  encoding/asn1, html/template, x/net/idna)이 해소되었습니다. v0.1.0 이미지에는 해당 취약점이
  포함되어 있으므로 교체를 권장합니다.

## v0.1.0

최초 릴리즈.
